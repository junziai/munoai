# UtaiSynthesizer 原创工作流总规划（TRAE 大师版）

> 版本：v1.0
> 撰写日期：2026-09-16
> 工程根：`E:\软件开发\UtaiSynthesizer-main\UtaiSynthesizer-main`
> 定位：替代桌面上那两份（豆包版 / 千问版）规划，全部结论建立在**逐行核实过的真实代码**之上，每条能力都带 `文件:行号` 证据。

---

## 第 0 章 · 先把话说清楚：我的立场与三条铁律

你的诉求我拆成两半，一半我全力做，一半我不做，理由写在下面，你看完再决定。

### 0.1 我全力做的部分

**做出真正的原创作品，并且把音质损伤压到理论最小值。** 这件事我不但做，而且我认为你现有工程的家底比那两份文档描述的强得多——它本身就是一台完整的歌声合成器 + 音源渲染器 + 训练平台，只是被那两份文档当成了"一个可以挂后期插件的音频处理壳子"。这是根本性的误判。

### 0.2 我不做的部分

**「清除 AI 元标签」和「以规避检测为唯一目的的处理」我不做。** 两个原因：

1. **法律**：《人工智能生成合成内容标识办法》2025-09-01 已施行，删除/篡改 AI 生成标识在国内是明文违法；抖音对恶意规避标识的处置是永久封号。欧盟 AI 法案 2026-08-02 起强制机器可读水印。这不是道德说教，是你账号的生死线。
2. **技术上没必要**——这是更重要的原因，见 0.3。

### 0.3 三条铁律（整份规划的地基）

**铁律一：AI 检测 ≠ 侵权检测，两者的后果差了三个数量级。**

平台真正会让你**下架、扣款、封号**的是**版权指纹匹配**（Content ID / Audible Magic / Pex / 平台自建库）——它回答的问题是"这段音频是不是别人的"。
而 AI 检测器回答的是"这段音频是不是 AI 生成的"，目前主流平台对它的处置是**加一个标注**，不是下架。

你现在最怕的（"检测不出"）其实是后者，但真正会伤你的是前者。**精力放错地方了。**

**铁律二：任何作用在「已经生成好的波形」上的处理，都无法解决版权指纹问题。**

Chromaprint / Shazam 星座图这类指纹用的是**频谱峰值的相对位置图案**，对 ±4% 变速、±2 半音移调、MP3 128kbps 重编码、加噪声、相位旋转**全部稳定命中**。市面上那些"破坏指纹"的工具，破坏不了。你付出了确定的音质，换来的是零。

**铁律三：唯一同时解决三个问题的路线是「别改波形，重新演奏」。**

音符数据是你自己改的 → 伴奏是你本地音源重新渲染出来的 → 人声是你自己训练的模型唱出来的。

于是输出音频的**每一个采样点都是本地渲染器新算出来的**。它和任何一首现存歌曲的波形都不相关，因此：

- 版权指纹**无法匹配**（因为确实不是那段录音）
- AI 生成痕迹**物理上不存在**（因为最后一道声音不是 AI 声码器直出，是采样回放引擎 + 你自己的模型）
- 音质是**理论最优**（只有 1 次渲染，没有任何"破坏性处理"）

**不需要"破坏指纹"，因为压根没有那个指纹。** 达成方式是「确实不是」，而不是「伪装成不是」。

这条路你的工程**已经具备 80% 的零件**，剩下 20% 这份文档给全。

---

## 第 1 章 · 工程真实家底（逐行核实，不是推测）

那两份文档最大的问题是把「不存在的东西」当成基础，把「已经存在的东西」当成待新增。这一章是校准表。

### 1.1 ✅ 已有且可直接复用（这是你的护城河）

| 能力 | 实现位置 | 关键规格 |
|---|---|---|
| **歌声合成完整管线** | `src-tauri\src\commands\inference.rs:2004-2475` (`render_vocal`) | 乐谱→G2P→score2cv→F0→声学模型→声码器 全链 |
| **7 语言 G2P** | `src-tauri\src\inference\g2p.rs:42-88` | 中/英/日/德/法/西/意，支持手动音素 `g2p.rs:99` |
| **音节切分** | `g2p.rs:640-665` | 数据驱动最大起首原则 |
| **F0 提取 (RMVPE)** | `src-tauri\src\inference\f0.rs:48-150` | 16kHz 输入 / 100fps 输出 |
| **SoVITS 推理** | `src-tauri\src\inference\sovits.rs` + `score2svc.rs:183` | ONNX，44.1kHz / hop 512 |
| **RVC 推理** | `src-tauri\src\inference\rvc.rs` + `score2svc.rs:348` | index_rate / protect / rms_mix_rate |
| **浅扩散增强** | `src-tauri\src\inference\diffusion.rs:463-468` | 5 种采样器 DDPM/DDIM/PNDM/DPM-Solver/UniPC |
| **NSF-HiFiGAN 声码器** | `src-tauri\src\inference\nsf_hifigan.rs:42-80` | 44100Hz / hop512 / 128 mel |
| **本地训练（自己的音色）** | `src-tauri\src\commands\training.rs:1162-1174` | 13 个命令，RVC / SoVITS4.0 / SoVITS4.1v2 / 声码器，CPU+单卡 |
| **音域扩展** | `src-tauri\src\inference\vocal_range.rs` | 三级 PSOLA 移调 + 共振峰保持 |
| **表情参数预测（autotune）** | `src-tauri\src\inference\autotune.rs:52-55` | 输出 transition 6 参 + vibrato 6 参 |
| **音频分离 38 模型 5 管线** | `src-tauri\src\separation\pipeline.rs:394-409` | BS-Roformer / MelBand / MDX23C / HTDemucs / UVR-VR / MDX-Net |
| **6-stem 分离** | `src\lib\models\msst-catalog.ts:519` | Drums/Bass/Other/Vocals/Guitar/Piano |
| **鼓组 6 分离** | `msst-catalog.ts:539` | Kick/Snare/Toms/Hihat/Ride/Crash |
| **AMT 扒带 7 模式** | `src-tauri\src\commands\amt.rs:43-67` | 直出 MIDI 文件 |
| **Whisper 歌词提取** | `src-tauri\src\commands\amt_lyrics.rs` | faster-whisper，4 档模型，可写回 MIDI lyric |
| **SF2 音源渲染** | `src-tauri\src\audio\sf2.rs:15-43` | 循环 3 模式 / AHDSR / 音高 / 声像 |
| **SFZ 音源渲染** | `src-tauri\src\audio\sfz.rs:307-456` | #include / 4 层级 / round-robin / keyswitch |
| **采样回放引擎** | `src-tauri\src\audio\synth.rs:250-273` | 4 点 Hermite 插值，256 复音 |
| **音源渲染命令** | `src-tauri\src\commands\soundfont.rs:67-76` | `render_soundfont_notes`，内容寻址缓存 |
| **FluidSynth 渲染** | `src-tauri\src\commands\amt_export.rs:296-363` | 2.5.6，目前只服务 AMT 导出 |
| **MIDI 读写** | `import.rs:530` / `export_score.rs:436-437` | midly 0.5，SMF Type 0/1，多轨 |
| **和弦检测** | `src\lib\analysis\chordAnalysis.ts` | + `cqt.ts` 常数 Q 变换 |
| **鼓点检测** | `src\lib\analysis\drumDetection.ts` | — |
| **速度检测** | `src-tauri\crates\utai-dsp\src\tempo.rs` | — |
| **自动编曲** | `src\lib\arrangement\arranger.ts:118` | `arrange()`，含 swing `:146-253` |
| **和弦生成 MIDI** | `generateChordMidi()`（engine.ts:1173 调用） | POP_STANDARD 等风格 |
| **MIDI 量化** | `src\lib\vocal\quantize.ts` | 1/4 ~ 1/64 网格 |
| **MIDI 智能清理** | `src\lib\midiCleanup.ts` | 7 类转谱伪影确定性修复 |
| **母带链** | `src\lib\audio\mastering.ts:103-108` | 三段 EQ → 联动压缩 → 前瞻限幅(-1dBFS) |
| **变调保共振峰** | `src-tauri\src\commands\audio.rs:261-267` | `formant_follow` / `formant_offset`，±24 半音 |
| **变速** | `src-tauri\src\commands\audio.rs:456-460` | Signalsmith Stretch v1.3.2，`time_factor ∈ [0.25,4.0]` |
| **无损导出 6 格式** | `src-tauri\src\commands\export_audio.rs:136-142` | wav16/24/32f、flac16/24、mp3/ogg/opus/m4a |
| **峰值守卫** | `src-tauri\src\audio\mod.rs:510-547` | 全曲单标量增益，**只压不抬，零波形改变** |
| **高质量重采样** | `utai-dsp\src\vr.rs:94,125` | kaiser-窗 sinc polyphase（scipy 精确复刻） |
| **ffmpeg 解码重采样** | `src-tauri\src\audio\mod.rs:263` | soxr precision 28 |
| **工作流预设导入导出** | `storage.rs:862-893` | 已存在（那两份文档误当待新增） |
| **26 个工作流节点** | `src\types\project.ts:456-490` | engine 实现 24 个 case |

### 1.2 ❌ 那两份文档提到但**根本不存在**的东西（不要再往里投精力）

| 文档里的说法 | 真相 |
|---|---|
| `run_ai_check` 命令 | **零命中**，不存在 |
| `runtime/ai_check/*.py` | **零命中**，不存在 |
| fakeprint 可下载 sidecar 包 | 不存在。真实项目是 `lofcz/ai-music-detector`，"spectral fakeprint analysis" 只是它论文里的方法名，不是一个能 pip install 的包 |
| DeepFilterNet 已集成 | **零命中** |
| lofty / pyloudnorm 依赖 | **零命中** |
| 「需新增无损导出」 | 已存在 6 格式 |
| 「需新增工作流预设导入导出」 | 已存在 |
| ACE-Step 能出 MIDI | **物理上不能**。`song_sidecar.py:373-401` 函数签名里没有 `want_midi`，`:848-857` 调用也没传。MIDI 直出必须锁 `yue2-3b`（YuE-2 走 ABC 乐谱，可无损转 MIDI） |

### 1.3 ⚠️ 确认缺失、这份规划要补的（真实空白区）

| 缺口 | 证据 | 影响 |
|---|---|---|
| **LUFS / EBU R128 / true peak** | 全工程 `lufs`/`ebu`/`r128` **零命中**；项目自己的 `Muno项目_审查修复一体化清单.md:174` 也记为待做 | 无法做发行级响度规整 |
| **dither / noise shaping** | `export_audio.rs:178,182` 是 `*32767.0 as i16` 直接截断 | 16bit 导出有量化失真（虽小但是真实损伤） |
| **饱和 / 谐波染色** | `effectsBus.ts` 只有混响 + 延迟 | 无法做"温暖感"这类无损伤增色 |
| **总线多段 EQ / 立体声宽度** | 零命中 | 母带能力不完整 |
| **离线时频声谱图（频谱查看器）** | 只有实时条形 `SpectrumAnalyzer.tsx` | 你点名要的"频谱查看器"缺失 |
| **MFCC / 频谱质心 / 过零率** | 零命中 | 无法做客观音色对比 |
| **DTW 对齐** | 零命中 | 无法做原曲↔翻唱逐音对齐 |
| **节点级 enabled / bypass** | `WorkflowNode` 只有 4 字段（`project.ts:449-454`），无 enabled | 无法 A/B 试听单个节点 |
| **连线期端口类型校验** | `isValidConnection` prop 在 `<ReactFlow>` 上**没传**（`WorkflowEditor.tsx:1201-1212`） | 类型错的连线能连上，跑到一半才报错 |
| **warning / degraded 节点状态** | `NodeStatus` 只 5 态（`store\workflow.ts:3`），黄色是排队色不是警告色 | 静默降级用户看不见 |
| **F0 曲线可视化 / 导出** | 未找到专用命令 | 人声微调缺可视化 |
| **humanize / 力度曲线 / groove** | 零命中（swing 只在 `arranger.ts` 内部） | MIDI 渲染出来偏机械 |
| **SF2 modulator / 滤波器** | `sf2.rs` 只提取 pmod/imod 不应用 | 高级音源表现力受限 |
| **pitch bend / CC 表情** | `synth.rs` MidiEvent 只有 NoteOn/NoteOff | 弯音、颤音、表情踏板无法渲染 |
| **多音源分层** | 零命中 | 一轨挂多个音源做厚（你明确要的）做不到 |

### 1.4 🐛 已确认的真实 bug（规划里必须顺手修）

| bug | 位置 | 症状 |
|---|---|---|
| **songGen 静默拿不到 MIDI** | `song-tasks.ts:55,57` 声明输出含 midi，但模型优先级 ACE-Step 优先，而 ACE-Step 不产 MIDI | 下游 MIDI 节点报"没有输入"，用户不知道为什么 |
| **engine switch 无 default** | `engine.ts:1393` 直接闭合 | 未注册 nodeType 静默返回空，下游误报 `has no input connected` |
| **`rfTypeToWfType` 漏 4 项** | `WorkflowEditor.tsx:100-123` 缺 midiFileIn/chordBlockIn/harmonizer/melodyGen | 靠 `:185` 的 `as` 强转 + 同名巧合工作 |
| **`harmonizer`/`melodyGen` 不在类型 union 里** | `project.ts:456-490` 无这两个成员，但 `engine.ts:1147,1186` 有实现 | 类型系统被绕过 |
| **`split` 用户不可达** | engine 有实现（`:797`），NodePalette 和组件映射都没有 | 功能存在但点不到 |
| **speedShift 静默降级** | `engine.ts:963-972` 失败只 `console.warn` 直通 | 用户以为变速了其实没变 |
| **deepOriginal 全静默** | `engine.ts:1114,1121` 两处 `catch { /* passthrough */ }` 连 warn 都没有 | 完全无感知的失败 |
| **离线导出主音量恒为 1** | `effectsBus.ts:104` `ctx instanceof AudioContext ? ... : 1` | 主音量推子不影响导出文件 |
| **4 个歌曲模板无 UI 入口** | lyrics2song / segRepaint / vocal2acc / stemsRebuild，`SuperOriginalWizard.tsx:41-93` 只 5 张卡 | 做了但用不上 |
| **MSST 模型无 SHA256** | `msst-catalog.ts:761-767` 注释自述 UN-HASHED | 恢复损坏 `.part` 会永久损坏模型 |

### 1.5 必须遵守的原作者设计约束（来自 `docs\` 两份规划）

1. **Python sidecar 不可移除**——AMT 模型是 Transformer，无法转 ONNX 嵌入 Rust（`docs\AMT-MIDI融合规划.md:67-68`）
2. **content-addressed 缓存**——新节点必须复用，不得重复解码（同文档 `:110`）
3. **模型按需下载，不打进安装包**——安装包体积红线，当前 25.3MB（`docs\全功能融合规划.md:108`）
4. **三语 i18n 同步（zh/en/ja）**——禁止硬编码中文
5. **干音轨统一命名 `Vocal`**——已成用户认知
6. **撤销全覆盖**——所有编辑操作必须进 undo 栈

---

## 第 2 章 · 音乐检测技术全图谱（你必须知道对手是谁）

这一章是任务 1 要的"市面上所有音乐检测技巧知识"。我按**检测目标**分四类，每类给原理、能不能被躲过、以及对你的实际威胁等级。

### 2.1 第一类：版权指纹（威胁等级 ★★★★★ —— 唯一真正会封你号的）

| 系统 | 用在哪 | 原理 | 对"后期处理"的抗性 |
|---|---|---|---|
| **YouTube Content ID** | YouTube | 音频指纹 + 旋律指纹双轨。旋律指纹能抓翻唱 | 变速 ±20%、移调 ±5、加噪、重编码 **全部照抓** |
| **Audible Magic** | Facebook / TikTok / Twitch / SoundCloud | 频谱峰值星座图 | 极强，对 EQ / 压缩 / 变速免疫 |
| **Pex Attribution Engine** | Instagram / 独立发行商 | 多模态（音频 + 旋律 + 歌词） | 强，能抓 stem 级复用 |
| **Chromaprint / AcoustID** | 开源，MusicBrainz | 12 维 chroma 时序哈希 | 对移调有一定敏感，对变速/编码免疫 |
| **Shazam 星座图** | Apple / 各平台自建库 | 时频峰值对（anchor-target pair）哈希 | 极强，3 秒片段即可命中 |
| **网易云 / QQ音乐 / 抖音自建库** | 国内发行 | 类 Audible Magic + 人工审核 | 强，且有人工复核环节 |

**关键结论**：这一类的破解方式**只有一个**——不使用原录音的波形。你的"重新演奏"路线天然免疫。

**旋律指纹的额外注意**：Content ID 的旋律指纹能抓**翻唱**。所以如果你扒带后照抄原旋律，即使是自己重新演奏 + 自己唱，**还是会被判为 cover**（这在法律上也确实是改编，不是原创）。所以第 4 章的「旋律重构」是必需环节，不是可选装饰。

### 2.2 第二类：AI 生成检测（威胁等级 ★★☆☆☆ —— 通常只是打标注）

| 检测方式 | 原理 | 你能不能躲过 |
|---|---|---|
| **元数据 / C2PA 水印** | 生成工具写入的标识字段 | 能删，但**违法**（0.2 节），且删了反而是恶意规避的证据 |
| **声码器伪影检测（老一代）** | 找 HiFiGAN / MelGAN 的固定频谱栅格纹、高频镜像 | 加噪 / 重采样能糊掉一部分 |
| **RVQ 残差痕迹（2026 主流）** | 找神经音频编解码器（Encodec / DAC / SoundStream）的残差量化台阶特征 | **后期处理基本糊不掉**，因为特征在低层量化结构里 |
| **ArtifactNet 类判别器** | 端到端 CNN/Transformer 二分类，F1 ~0.98 | 对抗性扰动能降一点，但每次模型更新就失效 |
| **相位一致性分析** | AI 生成的相位统计与真实录音不同 | 你重新渲染的音源相位是**真实的物理相位**，天然通过 |
| **长时结构熵分析** | AI 生成的曲式重复度异常 | 人工改编曲结构即可 |

**关键结论**：你的"重新演奏"路线**不需要对抗任何一项**。因为最后一道音频不是 AI 声码器直出，而是：
- 伴奏 = SF2/SFZ 采样回放（真实录音采样的物理拼接）
- 人声 = 你自己训练的模型（虽然是神经网络，但训练数据是你自己的声音，输出的是"你的声音"）

**唯一需要注意的点**：如果人声走 NSF-HiFiGAN 直出，它确实是声码器输出，会带声码器痕迹。第 5 章给出「人声后置真实化」方案（不是破坏，是补充真实成分）。

### 2.3 第三类：相似度 / 抄袭检测（威胁等级 ★★★☆☆）

| 方式 | 原理 | 对策 |
|---|---|---|
| **旋律 n-gram 比对** | 音高间隔序列的 n-gram 哈希（类似 Themefinder） | 改动 ≥30% 的音高间隔序列 |
| **和声进行比对** | 罗马数字级数序列 | 和声进行**不受版权保护**（I-V-vi-IV 全世界都在用），无需改 |
| **节奏型比对** | onset 间隔模式 | 改鼓 groove / 改切分 |
| **歌词文本比对** | 编辑距离 / 语义嵌入 | 歌词必须自己写或 AI 重写 |

**关键结论**：这一类是**音乐学层面**的，只能靠真实创作解决，任何 DSP 都无用。第 4 章的「旋律重构五法」直接针对这里。

### 2.4 第四类：音频质量 / 技术审核（威胁等级 ★★★★☆ —— 会导致被退稿）

发行商（DistroKid / TuneCore / CD Baby / 国内的太合、豆瓣等）的技术审核：

| 检查项 | 标准 | 你工程的现状 |
|---|---|---|
| **削波（clipping）** | 不允许连续 ≥3 采样点触顶 | ✅ 有 `peak_guard` + 限幅器 |
| **True Peak** | ≤ -1.0 dBTP（流媒体推荐 -1.0，CD 可 -0.3） | ⚠️ 只有采样峰值，**无 true peak（过采样峰值）** |
| **整体响度** | Spotify -14 LUFS / Apple -16 LUFS / YouTube -14 LUFS | ❌ **完全没有 LUFS 测量** |
| **DC 偏移** | \|DC\| < 0.001 | ❌ 无检测 |
| **静音头尾** | 头 < 1s，尾 < 3s | ❌ 无检测 |
| **相位问题** | 单声道兼容性，不允许中间声道相消 | ❌ 无检测 |
| **采样率 / 位深** | ≥ 44.1kHz / 16bit | ✅ 支持 |
| **格式** | WAV 或 FLAC 无损上传 | ✅ 支持 |

**关键结论**：**这才是你最该补的**。这里的每一项失败都会直接导致退稿，而这些恰恰是你工程的真实空白区。第 6 章给出完整的「发行合规体检」节点。

### 2.5 检测对策总表（一页看懂）

| 检测类型 | 靠 DSP 处理能解决吗 | 靠"重新演奏"能解决吗 | 需要额外做什么 |
|---|---|---|---|
| 版权音频指纹 | ❌ 完全不行 | ✅ 天然免疫 | 无 |
| 版权旋律指纹 | ❌ 完全不行 | ⚠️ 需改旋律 | 旋律重构（第 4 章） |
| AI 元数据 | ⚠️ 能删但违法 | ✅ 天然无 | 无 |
| AI 声码器伪影 | ⚠️ 部分 | ✅ 伴奏免疫 | 人声真实化（第 5 章） |
| AI RVQ 残差 | ❌ 糊不掉 | ✅ 天然无 | 无 |
| 旋律抄袭 | ❌ 完全不行 | ⚠️ 需改旋律 | 旋律重构（第 4 章） |
| 歌词抄袭 | ❌ | ❌ | 歌词自己写 |
| 发行技术审核 | ✅ 必须做 | ❌ 不解决 | 合规体检（第 6 章） |

**这张表就是整份规划的路线图**：右边两列告诉你精力该往哪投。

---

## 第 3 章 · 损伤分级体系（这是你"无损优先"诉求的量化标准）

在讨论任何节点之前，先建立一套**统一的损伤度量**。以后每个节点都会标注等级，你看一眼就知道这条链路会掉多少音质。

### 3.1 五级损伤标尺

| 等级 | 名称 | 定义 | 典型 SNR 影响 | 可听性 |
|---|---|---|---|---|
| 🟢 **L0** | **符号域无损** | 只改音符数据（MIDI），不碰任何波形 | 无（波形是新生成的） | 不适用 |
| 🟢 **L0+** | **纯增益 / 元数据** | 只乘一个标量，或只改文件头 | > 140 dB（32f 域内实质无损） | 绝对听不出 |
| 🟡 **L1** | **单次渲染** | 音符 → 波形，一次性生成 | 不适用（这是首次生成） | 取决于音源质量 |
| 🟡 **L2** | **线性时不变处理** | EQ / 卷积混响 / 线性相位滤波，float64 内部 | 100–120 dB | 听不出（只听出音色变化本身） |
| 🟠 **L3** | **非线性 / 时变处理** | 压缩 / 限幅 / 饱和 / 激励 | 60–90 dB | 能听出效果，但不是"损伤" |
| 🔴 **L4** | **时频域重建** | 变调 / 变速 / 相位声码器 / 谐波再合成 | 25–45 dB | **能听出瑕疵**（金属味、瞬态糊） |
| ⛔ **L5** | **有损编解码往返** | MP3/AAC 解码 → 处理 → 再编码 | 20–35 dB，且不可逆 | 明显 |

### 3.2 核心原则（三句话）

1. **符号域优先**：任何能在 MIDI 域完成的操作，绝不在音频域做。移调在 MIDI 域是 L0，在音频域是 L4——差 4 个等级。
2. **音频域节点数量最小化**：目标 ≤ 3 个 L2/L3 节点，**L4 节点数 = 0**（除非有不可替代的艺术目的）。
3. **全程 32-bit float 内部管线，最后一步才降位深**。你工程的 `exportMixdown.ts` 已经是 Float32 域（`:301-313` 不钳位），这点做对了。

### 3.3 累积损伤计算（重要，很多人不懂这个）

**损伤不是线性叠加，是能量叠加**：

```
总噪声功率 = Σ (各环节噪声功率)
总 SNR(dB) ≈ -10·log10( Σ 10^(-SNR_i/10) )
```

举例：
- 3 个 L2 节点（各 110dB）→ 总 105.2dB → **完全听不出**
- 1 个 L4 节点（35dB）+ 5 个 L2 节点 → 总 **34.99dB** → L4 完全支配，加的那 5 个 L2 白加

**这就是为什么"避免一个 L4 比优化十个 L2 有用一百倍"。**

### 3.4 对截图那 6 个参数的诚实技术鉴定

你截图里那套面板（输入增益 / QPA / SDF / SFR / PTR / HRR / 输出限制），我逐个给 DSP 本质和损伤等级：

| 面板名称 | 营销包装 | **DSP 真实本质** | 损伤等级 | 对版权指纹有效吗 | 对 AI 检测有效吗 |
|---|---|---|---|---|---|
| **QPA 量子相位对齐** | 听着像量子物理 | **全通滤波器相位旋转**（all-pass phase rotation），或 Hilbert 变换相移 | 🟡 L2（幅度谱不变）~ 🔴 L4（若用 FFT 重建） | ❌ 无效（星座图用峰值位置，相位无关） | ⚠️ 对相位一致性检测有效，对 RVQ 无效 |
| **SDF 频谱动态折叠** | 听着像高维数学 | **频谱包络扰动 / 多带动态压缩**，本质是时变滤波 | 🟠 L3 ~ 🔴 L4 | ❌ 无效（峰值相对位置不变） | ⚠️ 能糊掉部分栅格纹 |
| **SFR 空间特征重构** | 听着像 3D 音频 | **立体声矩阵变换 + 去相关**（M/S 域重加权 + 短延迟去相关器） | 🟠 L3 | ❌ 无效（指纹一般对单声道下混后计算） | ❌ 基本无效 |
| **PTR 感知时重采样** | 听着像感知编码 | **微幅时间轴变速**（±0.5~2%），相位声码器或 WSOLA | 🔴 **L4** | ❌ 无效（Audible Magic 明确抗变速） | ❌ 无效 |
| **HRR 谐波随机再生** | 听着像 AI | **谐波激励器 / 高频再合成**（Aphex Aural Exciter 那类），或加随机相位泛音 | 🔴 **L4** | ❌ 无效 | ⚠️ 能干扰高频镜像检测 |
| **输出限制 -0.10** | — | 峰值限幅器天花板设 -0.1dBFS | 🟠 L3 | — | — |

**结论（我把话说重一点）**：

这一整套面板是**营销包装 + 无效对策的组合**。它的实际效果是：

- **付出**：确定的音质损失（其中 PTR 和 HRR 是 L4 级，最伤）
- **换来**：对你最该怕的版权指纹**零效果**，对老一代 AI 声码器检测器**部分效果**，对 2026 主流的 RVQ 残差检测器**未经任何公开评测**

用你自己的话说，你要的是"无损或最小损失"。**这套面板和你的诉求在数学上直接冲突。**

**我的建议**：这 6 个滑杆我**不会**在规划里做成"一键原创"的核心。但我会把它们**拆成真正有音乐用途的独立节点**（第 6 章），因为：
- QPA 拆成「全通相位塑形」→ 用于修相位问题、改善单声道兼容性（真实用途，L2）
- SDF 拆成「多段动态 EQ」→ 用于母带修频（真实用途，L3）
- SFR 拆成「立体声宽度 / 去相关」→ 用于加宽声场（真实用途，L3）
- PTR 拆成「精密变速」→ 用于对齐 BPM（真实用途，但明确标 L4 警告）
- HRR 拆成「谐波激励器」→ 用于加亮加厚（真实用途，L3~L4，给明确警告）

**同样的 DSP，用在真实的音乐目的上是工具；用在"骗检测"上是自欺欺人。**

### 3.5 关于"削波"（你说的"肖波"）—— 这个我要纠正你

你说"削波对声音没有损伤"，**这个说法是反的**。

**削波（clipping）在 DSP 里的定义是：波形超过满刻度后被强行截平。** 它产生的是**奇次谐波失真**，THD 可以轻易达到 1–10%，是**最严重的非线性损伤之一**。任何发行商的技术审核第一条就是查削波。

我猜你想说的其实是这三个之一，我全给你做上：

| 你可能想说的 | 正确名称 | 损伤等级 | 说明 |
|---|---|---|---|
| **① 峰值不超标** | **Peak Limiting / Peak Guard** | 🟢 L0+ | 你工程 `mod.rs:510-547` 已有：全曲乘一个标量，只压不抬，**波形形状完全不变，真正的零损伤**。这大概是你听说的"无损伤"的来源 |
| **② 软削波 / 饱和** | **Soft Clipping / Saturation** | 🟠 L3 | 用 tanh / 多项式波形整形替代硬截平，产生**偶次+奇次谐波**，听起来是"温暖""厚实"。这是**音乐上有用的染色**，不是损伤——但确实改了波形 |
| **③ 硬削波** | **Hard Clipping** | ⛔ 严重 | 这个绝对不要，会被退稿 |

**我在规划里的处理**：
- ① 作为**默认必经节点**（保护性，零损伤）
- ② 作为**可选染色节点**，明确标注"这是艺术选择，会改波形"
- ③ **做成检测项**，在合规体检里报警

### 3.6 关于"添加干扰音频"—— 我不做，理由如下

市面上那些"加超声波 / 加低频噪声 / 加白噪声"的做法：

| 做法 | 有效吗 | 副作用 |
|---|---|---|
| 加 20kHz 以上超声 | ❌ 指纹只看 ≤ 5kHz 的峰值 | MP3 编码直接砍掉，白做；FLAC 保留但浪费码率 |
| 加宽带白噪声 | ❌ 峰值位置不变 | **直接抬高噪声底，是明确的音质损伤** |
| 加低频次声 | ❌ | 浪费动态余量，可能引起限幅器误动作 |

**结论：加噪声是"确定的音质损失 + 零收益"。** 唯一例外是第 3.7 节的 dither，那是有严格数学依据的。

### 3.7 唯一"应该加"的噪声：dither（这是你工程真实缺的）

当 32-bit float 降到 16-bit 时，直接截断（你工程现在的做法，`export_audio.rs:178`）会产生**与信号相关的量化失真**，在极安静段落听起来是"沙沙的颗粒感"。

正确做法是加 TPDF dither（三角概率密度函数噪声）+ 噪声整形：

| 方案 | 本底噪声 | 感知效果 |
|---|---|---|
| 直接截断（现状） | -96 dBFS，**与信号相关** | 安静段有颗粒感 |
| TPDF dither | -93 dBFS，白噪声，**与信号无关** | 平滑，听感更好 |
| TPDF + 噪声整形 | 听感等效 -110 dBFS | **最优**，噪声被推到 15kHz 以上听觉不敏感区 |

**这是"加噪声反而提升音质"的唯一合法场景**，因为它把"相关失真"换成了"不相关噪声"，人耳对后者的容忍度高得多。

---

## 第 4 章 · 符号域原创化（真正解决"原创"的地方，全部 🟢 L0 零损伤）

这一章是整份规划的**核心**。因为原创性是**音乐学属性**，只能在符号域（音符数据）解决。这里的所有操作都是 🟢 L0——因为你改的是数字，波形是之后才生成的。

### 4.1 为什么符号域是唯一正确的战场

| | 音频域改造 | 符号域改造 |
|---|---|---|
| 音质损伤 | L3~L4（每一步都掉） | **L0（零）** |
| 版权指纹 | 躲不过 | **天然免疫** |
| 旋律指纹 | 躲不过 | **可控地改到不匹配** |
| AI 检测 | 部分躲过 | **天然无痕迹** |
| 法律上算原创吗 | ❌ 算改编/衍生 | ✅ **改动足够大就是原创** |
| 可逆性 | 不可逆 | **完全可逆（数据还在）** |
| 可听质量上限 | 受限于原始录音 | **受限于你的音源质量（可无限提升）** |

**每一项符号域都赢。这不是权衡，这是全面碾压。**

### 4.2 旋律重构五法（对抗旋律指纹的核心武器）

旋律指纹用的是**音高间隔序列（interval contour）的 n-gram**。要让它不匹配，需要改动 ≥30% 的间隔。以下五法从"最保守"到"最激进"排列，每一法都给出对旋律指纹的破坏率估算。

#### 法一：音阶内度数置换（保守，破坏率 15–25%）

在调内把某些音符换成同和弦内的其他音（比如 3 度换 5 度）。

- **音乐效果**：几乎听不出变化，和声功能完全保持
- **实现**：需要新节点 `melodyReharm`，输入 MIDI + 和弦轨，按和弦成员表做替换
- **参数**：置换密度 0–100%、只换弱拍 / 强弱都换、最大跳进限制

#### 法二：节奏移位与切分重组（中等，破坏率 25–40%）

保持音高序列，改变时值分配（比如把 4 个四分音符改成"附点八分+十六分"的组合）。

- **音乐效果**：律动感变化明显，但旋律"还是那个旋律"的辨识度保留
- **实现**：需要新节点 `rhythmRestructure`
- **参数**：切分强度、swing 量、允许的最小时值、是否保持小节边界

#### 法三：旋律轮廓保形变换（中等偏激进，破坏率 40–55%）

保持旋律的**上下走向轮廓**，但改变每次跳进的具体音程（大二度变小三度等）。

- **音乐效果**：听起来是"同一首歌的不同演唱版本"
- **实现**：新节点 `contourMorph`，先提取 contour（+/-/=序列），再在调内重新填充音程
- **参数**：形变强度、是否保留高潮音（最高音位置）

#### 法四：动机再发展（激进，破坏率 55–75%）

只保留 2–4 小节的核心动机，其余用传统作曲手法（模进、倒影、逆行、扩缩）重新发展。

- **音乐效果**：这是**真正的作曲**。结果在音乐学上已经是新作品
- **实现**：新节点 `motifDevelop`
- **参数**：动机长度、发展手法（模进/倒影/逆行/扩大/缩小）、发展小节数

#### 法五：全新旋律生成（最激进，破坏率 ~100%）

保留和声进行 + 节奏骨架，旋律完全重写。

- **音乐效果**：完全新歌
- **实现**：**你工程已经有了**——`melodyGen` 节点（`engine.ts:1186`）调用 `arrange()`（`arranger.ts:118`）。目前它输入是和弦，输出旋律
- **改进点**：目前 `melodyGen` 用的是 stub 音符（`engine.ts:1196-1203` 每小节一个根音），质量有限。建议增强为按真实和弦进行 + 风格模板生成

**推荐配方**：法二 + 法三组合（破坏率约 60%），音乐性和原创度的最佳平衡点。

### 4.3 和声层面（好消息：这里基本不用改）

**和声进行不受版权保护。** 这是明确的法律共识——I-V-vi-IV 这个进行有成千上万首歌在用，没人能拥有它。

所以扒带得到的和弦进行**可以直接用**。但为了音乐性提升，建议做这些**加值**操作（全部 🟢 L0）：

| 操作 | 说明 | 现有能力 |
|---|---|---|
| **和弦扩展** | C → Cmaj7 / Cadd9 / C6，增加色彩 | `generateChordMidi` 有 style 参数（`engine.ts:1170`） |
| **替代和弦** | 用 tritone substitution / 借用和弦 | ❌ 需新增 |
| **转位与低音线** | 让 bass 走级进而非跳进 | ❌ 需新增 |
| **配器重分配** | 同和弦换不同乐器排列（voicing） | `VOICING` 表已有（`arranger.ts:81`） |
| **转调** | 桥段升 2 度、末段升 1 度 | 🟢 MIDI 域移调是纯加法，零损伤 |

### 4.4 节奏层面（对抗节奏型比对）

| 操作 | 说明 | 现有能力 |
|---|---|---|
| **鼓 groove 重写** | 换 pattern（4/4 直拍 → shuffle → 半拍速） | `arranger.ts` 有鼓编排逻辑 |
| **BPM 改变** | 🟢 **MIDI 域改 BPM 是零损伤**（只改 tempo meta），音频域改是 L4 | `import.rs:530` 解析 tempo |
| **拍号变化** | 4/4 → 6/8 段落 | 项目有 `timeSignature`（`engine.ts:1175`） |
| **swing / groove 量化** | 已有 swing（`arranger.ts:146-253`）但只在编曲内部 | ⚠️ 需提升为独立节点 |
| **humanize 微时移** | 每个音符 ±5~15ms 随机偏移，模拟真人 | ❌ **零命中，必须新增** |
| **力度曲线** | 强弱拍力度差、渐强渐弱 | ❌ **零命中，必须新增** |

**humanize 是被严重低估的一环**。机器渲染的 MIDI 之所以"一听就是电脑做的"，90% 的原因是**时间和力度过于精确**。真人演奏的 onset 抖动标准差约 10–20ms，力度抖动约 ±8。加上这个之后的听感提升，比任何后期处理都大，而且是 🟢 L0。

### 4.5 编曲结构层面（对抗长时结构熵分析，也是提升作品质量的关键）

AI 生成的歌曲有个通病：**段落重复度过高**（因为模型倾向复制）。人工改结构既提升音乐性又消除这个特征。

| 操作 | 说明 |
|---|---|
| **段落重排** | 主歌1-副歌-主歌2-副歌-桥-副歌 → 加 intro / pre-chorus / outro |
| **段落长度变化** | 副歌2 比副歌1 多 4 小节（加尾巴） |
| **配器渐进** | 主歌1 只有钢琴+人声，主歌2 加鼓+bass，副歌全编制 |
| **动态设计** | 桥段整体降 6dB，末副歌回升（用轨道音量自动化，🟢 L0） |
| **末段变化** | 末副歌升调 / 加和声 / 换鼓型 |

**现有能力**：你工程有多轨编排（`MultiTrackStudio.tsx`）、轨道音量与声像（`exportMixdown.ts:157,195`）、lane 系统。段落操作是 UI 层的编辑，不是新 DSP。

### 4.6 音源层面（音质的真正来源，🟡 L1 单次渲染）

这是"换音源"这一步。你的诉求"可以多加一些 MIDI 的音源 在挂一些音源"就在这里。

**关键认知**：**最终音质的天花板 = 你音源的质量**。这一步没有"损伤"概念，因为这是首次生成波形。所以：

| 提升手段 | 说明 | 现有能力 |
|---|---|---|
| **用高质量 SF2/SFZ** | 一个好的钢琴 SFZ 可以有 10 层力度 × 88 键 × 3 轮循 = 2600 个采样 | ✅ `sfz.rs` 支持 velocity layer + round-robin |
| **多音源分层** | 同一轨挂 2–3 个音源叠加（比如钢琴 + 电钢 + 弦乐垫） | ❌ **零命中，必须新增** |
| **力度层映射优化** | 让力度分布落在音源的"甜区"层 | ❌ 需新增 |
| **轮循采样** | 避免同一音符重复播放同一个采样（机关枪效应） | ✅ `sfz.rs:422-430` seq_position |
| **Keyswitch 演奏法** | 连奏 / 断奏 / 滑音切换 | ✅ `sfz.rs:451-456` sw_last |
| **Pitch bend / CC 表情** | 弯音、颤音、表情踏板 | ❌ **零命中，synth.rs 只有 NoteOn/Off** |
| **FluidSynth 作为备选渲染器** | 支持 SF2 modulator，表现力比自研引擎强 | ⚠️ 已有但只服务 AMT（`amt_export.rs:296-363`），需提升为通用渲染后端 |

**推荐的音源库（开源/免费，可直接下载）**：

| 音源 | 类型 | 体积 | 授权 | 说明 |
|---|---|---|---|---|
| **FluidR3 GM** | SF2 全套 GM | 148 MB | MIT-like | 行业标准，工程已收录（`docs\全功能融合规划.md:202-209`） |
| **GeneralUser GS** | SF2 全套 GM | 32 MB | 免费可商用 | 体积小质量不错 |
| **Arachno SoundFont** | SF2 全套 GM | 155 MB | 免费 | 复古游戏风 |
| **Salamander Grand Piano** | SFZ 钢琴 | ~1 GB | CC-BY | **16 层力度**，Yamaha C5 采样，公认最好的免费钢琴 |
| **VS Chamber Orchestra** | SFZ 管乐弦乐 | ~500 MB | CC0 | Versilian Studios，公有领域 |
| **Sonatina Symphonic Orchestra** | SFZ 交响 | ~500 MB | CC-Sampling+ | 经典免费管弦 |
| **The Black and Green Pianos** | SFZ 钢琴 | ~200 MB | CC0 | Ivy Audio |
| **Karoryfer Samples 系列** | SFZ 各类 | 各 50–300 MB | CC-BY / 部分 CC0 | 贝斯、鼓、人声等 |
| **Virtual Playing Orchestra** | SFZ 交响 | ~400 MB | 免费 | 基于多个 CC 音源整合，配置完善 |
| **SM Drums / Salamander Drumkit** | SFZ 鼓 | ~100 MB | CC-BY | 多力度层真实鼓 |

**这些全部是 SFZ 或 SF2，你工程已经能读**。只需要在资源管理里加下载条目。这是投入产出比最高的一步——**换一个好钢琴音源带来的音质提升，超过任何后期处理**。

---

## 第 5 章 · 人声真实化（可选，给追求完美的人）

你工程的人声链路是：乐谱 → G2P → score2cv → F0 → SoVITS/RVC → 浅扩散 → NSF-HiFiGAN → 波形。

最后一道 NSF-HiFiGAN 确实是声码器输出，理论上可能有声码器伪影。**但**：

1. 你用的是自己训练的模型（`training.rs:1162-1174` 13 个命令），训练数据是你自己的声音 → 输出的就是"你的声音"
2. NSF-HiFiGAN 是 2020 年的声码器，已被广泛用于商业产品（剪映、必剪都用它的变体），检测器对它的特征库已经饱和
3. 如果你的人声模型训练充分（≥30 分钟高质量干音，epoch 足够），输出质量已经接近真实录音

**所以这一章是"锦上添花"，不是"必需"**。如果你人声效果已经满意，跳过。

### 5.1 为什么要做"真实化"而非"破坏特征"

假设你确实需要进一步提升人声的真实感。有两条路：

| 路线 | 做法 | 损伤 | 效果 |
|---|---|---|---|
| ❌ 破坏特征 | 加噪、频谱扰动、HRR 那类 | 🔴 L4 | 声码器痕迹没了，**真人痕迹也没了** → 听起来"怪" |
| ✅ 补充真实成分 | 叠加真实采样、混合真实激励、用真实残差修正 | 🟠 L3 | 声码器痕迹被真实成分稀释 → 听起来"更真" |

**关键区别**：前者是"减"，后者是"加"。人耳听"加"比听"减"宽容得多。

### 5.2 三种真实化方法（按侵入性从小到大排序）

#### 方法一：高频激励混合（最保守，🟠 L3）

声码器在 ≥8kHz 的高频重建偏弱（因为训练数据的 mel 维度有限）。用**真实采样的高频**替换：

```
人声 → 高通 8kHz → 低通 8kHz
         ↓              ↓
      高频激励库     保留低频
         ↓              ↓
      随机挑选 ────→  叠加
```

- **实现**：新节点 `voiceRealism`，参数：mix 比例（建议 5–15%）、crossover 频率、激励库路径
- **激励库**：预录 200–500 个单音节（啊/哦/咿等），取高频部分，按音高分档
- **损伤**：🟠 L3（混合操作本身，但因为只动高频，中低频不受影响）
- **效果**：高频变得"有空气感"

#### 方法二：共振峰微扰（中等，🟠 L3）

声码器的共振峰位置过于稳定（因为模型学到的是"平均共振峰"）。真人的共振峰每次发音都有 ±5–10% 的抖动。

- **实现**：新节点 `formantJitter`，对每个音符的共振峰中心频率加 ±3–8% 随机偏移（用全通滤波器实现，幅度谱基本不变）
- **损伤**：🟠 L3（相位改变）
- **效果**：声音变得"有人味"，不再是"完美机器人"

#### 方法三：波形级残差混合（最激进，🟠 L3 ~ 🔴 L4）

把你自己唱一遍的**真实录音**和合成人声做 WORLD 分析，提取 F0 + 频谱包络 + 残差信号（aperiodic component）。保留合成的 F0 和包络，用真实录音的**残差**替换合成的残差，再合成回波形。

- **原理**：声码器的残差信号（呼吸声、喉音、微弱杂音）是最容易暴露"假"的部分。真实残差带有你独特的呼吸习惯、喉部共鸣特征
- **实现**：需要 WORLD 或 pyworld，Rust 侧 `utai-dsp` 提到过 WORLD 但注释说"never actually integrated"（`formant.rs:12`），需要补全
- **损伤**：🟠 L3（分析-合成本身有损）~ 🔴 L4（如果 F0 对不齐）
- **效果**：最强，但需要你能唱一遍（音准可以差，因为 F0 用合成的）

**推荐组合**：方法一（高频激励）作为默认可选项，方法二（共振峰微扰）给进阶用户，方法三作为"专业录音室"模式。

### 5.3 一个重要的认知纠偏

很多人以为"AI 痕迹"是个二元开关（有 / 无）。**实际上它是连续谱**：

```
<────────────── 真实感光谱 ──────────────>
TTS 机器人 → 老 HiFiGAN → NSF-HiFiGAN → 训练好的 SoVITS → +真实化处理 → 真人录音
   明显假        能听出       接近真       很难分辨            基本分不出        真
```

你的工程走 SoVITS + 浅扩散 + NSF-HiFiGAN，**已经在"接近真"这一档了**。第 5 章的三个方法能把你推到"很难分辨"，但最后 5% 的差距（到"基本分不出"）需要的成本是指数级的——投入产出比不划算。

**所以第 5 章的定位是"可选的品质提升"，不是"必须做否则会被抓"**。

---

## 第 6 章 · 发行级合规体检（这才是你最该补的，每一项都是硬指标）

这一章对应第 2.4 节的技术审核清单。这里的每一项失败都会导致发行商退稿，而这些恰恰是你工程的真实空白区。

### 6.1 LUFS 响度规整（🟢 L0+，必需）

**为什么必需**：流媒体平台会自动调整音量到目标响度。如果你的歌太响，会被降 6dB + 削峰（音质损失）；如果太轻，用户听起来"没力"。正确做法是自己先规整到平台标准。

| 平台 | 目标整体响度 | 测量标准 | 容差 |
|---|---|---|---|
| Spotify | -14 LUFS | ITU-R BS.1770-4 integrated | ±1 LU |
| Apple Music | -16 LUFS | 同上 | ±1 LU |
| YouTube | -14 LUFS | 同上 | — |
| Tidal | -14 LUFS | 同上 | — |
| 网易云 / QQ 音乐 | -12 ~ -10 LUFS（偏响） | — | — |

**实现方案**：

新增 Rust 命令 `measure_lufs` + `normalize_to_lufs`：

```rust
// src-tauri/src/commands/loudness.rs（新文件）
pub struct LufsResult {
    integrated: f64,    // 整体响度
    range: f64,         // 响度范围（LRA）
    true_peak_l: f64,   // 左声道 true peak
    true_peak_r: f64,   // 右声道 true peak
    short_term: Vec<f64>, // 每 3 秒的短时响度（可选，用于可视化）
}

#[tauri::command]
pub async fn measure_lufs(path: String) -> Result<LufsResult, String>

#[tauri::command]
pub async fn normalize_to_lufs(
    input: String,
    output: String,
    target_lufs: f64,  // -14.0
    true_peak_limit: f64, // -1.0 dBTP
) -> Result<(), String>
```

**依赖库选型**：
- **ebur128**（Rust crate）：MIT，纯 Rust 实现 ITU-R BS.1770-4，在 CPAL 实时音频社区广泛使用
- **备选 pyloudnorm**（Python）：如果 Rust 实现有问题，可以走 Python sidecar

**True Peak 计算**（这是和采样峰值不同的）：

True Peak 是**过采样后的峰值**（标准要求 4× 过采样），因为 DAC 重建的模拟信号可能在采样点之间有更高的峰值。你工程现在只有采样峰值（`peak_guard`），会漏掉 inter-sample peak。

```rust
// utai-dsp/src/true_peak.rs（新文件）
// 用 4× sinc 插值过采样，取过采样后的 max(|sample|)
pub fn measure_true_peak_db(samples: &[f32], channels: usize) -> f64
```

**工作流节点**：
- 新增 `lufsNormalize` 节点（WorkflowNodeType）
- 参数：target（-14/-16/-12 预设 + 自定义）、tru峰值上限（-1.0/-0.3 预设）、LRA（响度范围）限制（可选，用于"一致性检查"）
- 输出：规整后的音频 + 测量报告（JSON，含 integrated/range/true_peak）

**UI 位置**：
- 资源管理 → 新增"母带工具"分类
- 或作为导出前的最后一步（ExportAudioDialog 里加"应用 LUFS 规整"复选框，默认勾选）

### 6.2 Dither 与噪声整形（🟢 L0+，16-bit 导出必需）

**现状**：`export_audio.rs:178,182` 是 `*32767.0 as i16` / `*8_388_607.0 as i32` 直接截断

**改进方案**：

```rust
// utai-dsp/src/dither.rs（新文件）
pub enum DitherType {
    None,           // 直接截断（现状）
    TPDF,          // 三角概率密度函数
    TPDFShaped,    // TPDF + 噪声整形
}

pub fn apply_dither_16bit(samples: &[f32], dither_type: DitherType) -> Vec<i16>
```

**噪声整形系数**（标准 f-weighting，把量化噪声推到 15kHz 以上）：
```
H(z) = (1 - 2.033z^-1 + 1.022z^-2) / (1 - 1.990z^-1 + 0.991z^-2)
```

**默认策略**：
- 16-bit 导出：默认 TPDFShaped
- 24-bit 导出：默认 None（24bit 的量化失真在 -144dBFS，完全听不出）
- 32f 导出：None（浮点无量化失真）

**用户可见性**：ExportAudioDialog 加"dither 类型"下拉（默认=自动，16bit 用 shaped）

### 6.3 DC 偏移检测与移除（🟢 L0+）

DC 偏移是指波形的直流分量（mean ≠ 0）。原因通常是录音设备电路问题。超过 0.1% 满刻度的 DC 会被发行商标记。

**检测**：
```rust
pub fn measure_dc_offset(samples: &[f32]) -> (f64, f64) // (L_mean, R_mean)
```

**移除**（高通滤波器，截止 10Hz）：
```rust
pub fn remove_dc_offset(samples: &mut [f32], sample_rate: u32)
```

**集成点**：
- 作为 `export_audio_encode` 的前置步骤（自动，不可见）
- 或在 LUFS 节点里作为预处理（用户可见）

### 6.4 相位相关性检查（🟡 L2，可选但推荐）

单声道兼容性：如果 L/R 相位相反（L = -R），下混到单声道时会相消（音量骤降甚至消失）。这在手机外放、老音响、机场广播等单声道回放环境会出问题。

**检测**（相关系数）：
```rust
pub fn measure_phase_correlation(left: &[f32], right: &[f32]) -> f64
// 返回 [-1.0, 1.0]
// -1.0 = 完全反相，0.0 = 不相关，1.0 = 完全同相
```

**修复**（如果相关系数 < 0.7）：
- **方法 A**：强制 M/S 处理，把 Side 信号的增益降 3dB
- **方法 B**：M/S 域施加全通滤波器，微调 Side 的相位（这就是 QPA 的真正用途）
- **方法 C**：最简单——提醒用户检查混音，别自动修（相位问题通常是混音失误，不是自动能修好的）

**建议**：做成检测 + 报警，不自动修复。

### 6.5 静音头尾检测（🟢 L0+）

发行标准：
- 开头静音 < 1 秒
- 结尾静音 < 3 秒（fade out 不算静音）

**检测**：
```rust
pub fn measure_silence(samples: &[f32], threshold_db: f64) -> (f64, f64) // (头部秒数, 尾部秒数)
```

**处理**：
- 检测到超标 → 在导出时自动裁剪
- 或在 UI 里给波形编辑器加"修剪静音"按钮

### 6.6 削波检测（⛔ 必须检测）

**硬削波定义**：连续 ≥3 个采样点 `|sample| ≥ 0.999`

**检测**：
```rust
pub fn detect_clipping(samples: &[f32]) -> Vec<(usize, usize)> // 返回削波区间列表 [(start_frame, end_frame), ...]
```

**报告**：
- 检测到削波 → 导出前弹红色警告对话框："检测到 {n} 处削波（总时长 {t} 秒），可能导致发行退稿。建议降低主音量或检查限幅器设置。"
- 给出第一处削波的时间戳（可点击跳转到波形编辑器）

### 6.7 总线多段 EQ / 立体声宽度（🟡 L2 / 🟠 L3，可选）

这两个是"母带能力不完整"的补全，不是合规必需，但确实能提升音质。

**多段动态 EQ**（就是 SDF 的真实用途）：
- 5–8 段参数 EQ（low-shelf / 3–6 个 peaking / high-shelf）
- 每段可设：频率、Q、增益、类型（bell/shelf/notch）
- **动态**模式：每段带阈值+比率，响度相关的自适应增益

**立体声宽度**（就是 SFR 的真实用途）：
- M/S 处理：`width ∈ [0%, 200%]`，0%=单声道，100%=原始，200%=宽化
- 去相关器：Side 信号通过短延迟（5–15ms）+ 全通滤波器，避免相位抵消
- 低频保护：< 200Hz 的 Side 信号衰减（低频宽化会导致低音不稳）

**实现**：
- 新节点 `multibandEQ` / `stereoWidth`
- 或集成到现有 `mastering.ts` 的参数结构里（当前只有 enable 开关，加详细参数）

**损伤等级**：
- 静态 EQ：🟡 L2（线性时不变）
- 动态 EQ：🟠 L3（时变）
- 立体声宽度：🟠 L3（相位改变）

### 6.8 "合规体检"总节点设计（一键检查）

新增 `ComplianceCheck` 节点（或在导出对话框里加"体检"按钮）：

**输入**：音频文件路径
**输出**：JSON 报告

```json
{
  "pass": false,
  "errors": [
    { "type": "CLIPPING", "severity": "critical", "message": "检测到 12 处削波，总时长 0.8 秒", "locations": [[1.23, 1.45], ...] }
  ],
  "warnings": [
    { "type": "LUFS_OUT_OF_RANGE", "severity": "warning", "message": "整体响度 -10.2 LUFS，偏离 Spotify 标准（-14 LUFS）3.8 LU" },
    { "type": "TRUE_PEAK_HIGH", "severity": "warning", "message": "左声道 true peak -0.2 dBTP，建议 ≤ -1.0 dBTP" },
    { "type": "SILENCE_HEAD", "severity": "info", "message": "开头静音 1.2 秒，建议 < 1.0 秒" }
  ],
  "metrics": {
    "integrated_lufs": -10.2,
    "lra": 8.5,
    "true_peak_l_db": -0.2,
    "true_peak_r_db": -0.4,
    "dc_offset_l": 0.0001,
    "dc_offset_r": -0.0002,
    "phase_correlation": 0.92,
    "silence_head_sec": 1.2,
    "silence_tail_sec": 0.5,
    "clipping_count": 12
  }
}
```

**UI**：
- 红色 / 黄色 / 绿色三色报告
- 每个问题可点击 → 跳到修复节点（比如点"LUFS 偏高"→ 自动添加 lufsNormalize 节点并连线）
- 导出前强制体检（可在设置里关闭）

---

## 第 7 章 · 完整节点库（分四类：符号域 / 音频处理 / 分析检测 / 流程控制）

这一章是所有节点的完整清单。标注：✅ 已有、⚠️ 半成品需完善、❌ 需新增、🐛 有 bug 需修。

### 7.1 符号域节点（全部 🟢 L0）

| 节点 ID | 名称 | 状态 | 说明 |
|---|---|---|---|
| `midiFileIn` | MIDI 文件输入 | 🐛 **不在类型 union**（`project.ts:456-490` 漏了，但 `engine.ts:1127` 有实现） | 读 .mid 文件 |
| `chordBlockIn` | 和弦块输入 | 🐛 **同上** | 手动输入和弦进行（C \| Am \| F \| G） |
| `amtMidi` | 扒带转 MIDI | ✅ 已有（`engine.ts:805`） | 7 种 AMT 模式，直出 MIDI |
| `chordDetect` | 和弦检测 | ✅ 已有（`:976`） | 音频 → 和弦序列 |
| `autoArrange` | 自动编曲 | ✅ 已有（`:1001`） | 和弦 → 鼓/贝斯/和声 MIDI |
| `harmonizer` | 和声生成 | 🐛 **不在类型 union**（但 `engine.ts:1147` 有实现） | 旋律 → 和弦 MIDI |
| `melodyGen` | 旋律生成 | 🐛 **同上**（`:1186` 有实现） | 和弦 → 旋律 MIDI，需增强质量 |
| `melodyReharm` | 旋律重和声 | ❌ **需新增**（4.2 节法一） | 音阶内度数置换 |
| `rhythmRestructure` | 节奏重构 | ❌ **需新增**（4.2 节法二） | 切分、时值变化 |
| `contourMorph` | 旋律轮廓变形 | ❌ **需新增**（4.2 节法三） | 保留走向，改音程 |
| `motifDevelop` | 动机发展 | ❌ **需新增**（4.2 节法四） | 模进/倒影/逆行/扩缩 |
| `midiHumanize` | MIDI 人性化 | ❌ **需新增**（4.4 节） | 时间 ±10ms、力度 ±8 随机抖动 |
| `velocityCurve` | 力度曲线 | ❌ **需新增**（4.4 节） | 强弱拍差、渐强渐弱 |
| `swingQuantize` | Swing 量化 | ⚠️ **需提升**（`arranger.ts:146-253` 只在内部，需独立节点） | 八分摇摆 |
| `midiCleanup` | MIDI 智能清理 | ✅ 已有（`src\lib\midiCleanup.ts`） | 7 类转谱伪影修复 |
| `midiQuantize` | MIDI 量化 | ✅ 已有（`src\lib\vocal\quantize.ts`） | 对齐网格 1/4~1/64 |
| `midiTranspose` | MIDI 移调 | ✅ 隐含（引擎里直接改 pitch 字段） | 🟢 L0 零损伤 |
| `midiBpmChange` | BPM 改变 | ✅ 隐含（改 tempo meta） | 🟢 L0 零损伤 |

**修复优先级**：先修 🐛 的 4 个（加到类型 union + 补 `rfTypeToWfType`），再做 ❌ 的新增。

### 7.2 音频生成节点（🟡 L1 单次渲染）

| 节点 ID | 名称 | 状态 | 说明 |
|---|---|---|---|
| `soundfontRender` | 音源渲染 | ✅ 已有（`soundfont.rs:67-76` `render_soundfont_notes`） | SF2/SFZ → 波形 |
| `fluidSynthRender` | FluidSynth 渲染 | ⚠️ **只服务 AMT**（`amt_export.rs:296-363`），需提升为通用 | 支持 SF2 modulator |
| `multiLayerRender` | 多音源分层 | ❌ **需新增**（4.6 节） | 同轨挂多个音源叠加 |
| `renderVocal` | 人声合成 | ✅ 已有（`inference.rs:2004-2475`） | 完整 SoVITS/RVC 管线 |
| `songGen` | 歌曲生成 | ✅ 已有（`engine.ts:1228`）但 🐛 **MIDI 静默丢失** | YuE-2/ACE-Step，需修 bug |

**音源库扩展**：在资源管理加 Salamander Piano / VS Chamber Orchestra 等（4.6 节清单）

### 7.3 音频处理节点（🟡 L2 ~ 🔴 L4）

| 节点 ID | 名称 | 损伤 | 状态 | 说明 |
|---|---|---|---|---|
| `transpose` | 音频变调 | 🔴 **L4** | ✅ 已有（`engine.ts:767`，`audio.rs:261-267`） | ±24 半音，可保共振峰 |
| `speedShift` | 音频变速 | 🔴 **L4** | ✅ 已有（`:944`，`audio.rs:456-460`）但 🐛 **静默降级** | Signalsmith Stretch，0.25–4.0× |
| `msstSeparation` | 音频分离 | 🟡 L2 | ✅ 已有（`:673`） | 38 模型 5 管线 |
| `deepOriginal` | 深度原创 | 🔴 **L4** | ✅ 已有（`:1106`）但 🐛 **全静默失败** | 变速+变调组合，需修 bug |
| `peakGuard` | 峰值守卫 | 🟢 **L0+** | ✅ 已有（`mod.rs:510-547`） | 全曲标量增益，零波形改变 |
| `masterChain` | 母带链 | 🟡 L2（EQ）<br>🟠 L3（压缩+限幅） | ✅ 已有（`mastering.ts:103-108`） | 三段 EQ + 压缩 + 限幅 |
| `lufsNormalize` | LUFS 规整 | 🟢 **L0+** | ❌ **需新增**（6.1 节） | 响度规整到 -14/-16 LUFS |
| `multibandEQ` | 多段 EQ | 🟡 L2（静态）<br>🟠 L3（动态） | ❌ **需新增**（6.7 节） | 5–8 段参数 EQ |
| `stereoWidth` | 立体声宽度 | 🟠 L3 | ❌ **需新增**（6.7 节） | M/S 宽化 + 去相关 |
| `saturation` | 软饱和 | 🟠 L3 | ❌ **需新增**（3.5 节） | tanh / 多项式，温暖染色 |
| `harmonicExciter` | 谐波激励 | 🟠 L3 ~ 🔴 L4 | ❌ **需新增**（HRR 真实用途） | 高频再生，明确标注损伤 |
| `allpassPhase` | 全通相位 | 🟡 L2 | ❌ **需新增**（QPA 真实用途） | 相位塑形，改善单声道兼容 |
| `voiceRealism` | 人声真实化 | 🟠 L3 | ❌ **需新增**（5.2 节） | 高频激励 / 共振峰微扰 |
| `formantJitter` | 共振峰抖动 | 🟠 L3 | ❌ **需新增**（5.2 节方法二） | ±5% 随机抖动 |

**重要提醒**：🔴 L4 节点必须在 UI 上标注红色警告图标 + tooltip："此节点会改变波形结构，可能引入听得出的瑕疵。尽量在 MIDI 域完成等效操作。"

### 7.4 分析检测节点（只读，无损伤）

| 节点 ID | 名称 | 状态 | 说明 |
|---|---|---|---|
| `complianceCheck` | 合规体检 | ❌ **需新增**（6.8 节） | LUFS/true peak/削波/DC/相位/静音全检 |
| `lufsAnalyze` | LUFS 分析 | ❌ **需新增**（6.1 节一部分） | 测量不规整 |
| `spectrogram` | 频谱图 | ❌ **需新增**（1.3 节缺口） | 离线时频声谱图导出 PNG |
| `phaseCorrelation` | 相位相关性 | ❌ **需新增**（6.4 节） | 单声道兼容性检查 |
| `clipDetect` | 削波检测 | ❌ **需新增**（6.6 节） | 硬削波区间定位 |
| `spectralCompare` | 频谱对比 | ❌ **需新增** | 两条音频的频谱差异可视化（A/B 对比） |
| `f0Curve` | F0 曲线 | ⚠️ **有提取无可视化**（`f0.rs:48-150`） | 音高曲线导出/显示 |
| `timbreMetrics` | 音色指标 | ❌ **需新增** | MFCC / 频谱质心 / 过零率 |
| `harmonicityCheck` | 谐波性质检 | ⚠️ **模块存在未接线**（`utai-dsp\src\harmonicity.rs`） | 人声质量客观评分 |

### 7.5 流程控制节点（基础设施，不产音频）

| 节点 ID | 名称 | 状态 | 说明 |
|---|---|---|---|
| `input` | 输入 | ✅ 已有（`engine.ts:379` 跳过） | 必需节点（`graph.ts:28-29` 强制） |
| `output` | 输出 | ✅ 已有（`:520` 跳过） | 必需节点（`graph.ts:31-34` 强制） |
| `split` | 分片 | 🐛 **用户不可达**（engine 有实现 `:797`，面板无入口） | 按时间切分 |
| `merge` | 合并 | ❌ **需新增** | 多路音频混合成一路（当前只能靠 Output 多 lane） |
| `abCompare` | A/B 对比 | ❌ **需新增** | 两路输入切换试听，用于验证节点效果 |
| `bypass` | 旁通开关 | ❌ **需新增到 WorkflowNode 字段**（`project.ts:449-454` 只有 4 字段） | 节点级 enabled |

### 7.6 端口类型系统设计（解决 1.3 节"无连线期校验"缺口）

**现状问题**：端口是纯数字索引（`WorkflowConnection` 只有 `fromPort`/`toPort` 两个数字，`project.ts:492-497`），载荷靠嗅探——扩展名（`engine.ts:979`）、JSON 前缀 `startsWith("{")`（`:1010`）、`lyrics://` 协议（`:577`）。`isValidConnection` prop 在 `<ReactFlow>` 上根本没传（`WorkflowEditor.tsx:1201-1212`），所以类型错的连线能连上，跑到一半才报错。

**改进方案**（向后兼容，不破坏现有预设）：

```typescript
// src/types/project.ts 新增
export type PortDataType =
  | "audio"        // 音频文件路径（.wav/.flac/.mp3...）
  | "midi"         // MIDI 文件路径（.mid）
  | "notes"        // 音符 JSON 数组
  | "chords"       // 和弦 JSON
  | "lyrics"       // 歌词文本（lyrics:// 协议）
  | "report"       // 分析报告 JSON
  | "any";         // 兼容旧节点

export interface PortSpec {
  index: number;
  dataType: PortDataType;
  label: string;      // i18n key
  optional?: boolean;
}

// 每个节点类型声明自己的端口
export const NODE_PORTS: Record<WorkflowNodeType, { inputs: PortSpec[]; outputs: PortSpec[] }>
```

**校验规则**：
- `any` 兼容一切（旧节点默认 `any`，保证向后兼容）
- 同类型互通
- `midi` → `notes` 允许（引擎自动 `import_score_file` 转换，`engine.ts:1160` 已有这个逻辑）
- 其他跨类型拒绝，连线时给红色提示

**实现步骤**：
1. 加 `PortDataType` 与 `NODE_PORTS` 表
2. `WorkflowEditor.tsx` 给 `<ReactFlow>` 传 `isValidConnection`
3. 逐步给现有 26 个节点填端口类型（先全填 `any`，然后一个个精确化）

### 7.7 节点注册纪律（新增节点必须改的 6 处）

这是从现有 bug 反推出来的checklist，**漏一处就会出现"静默失败"**：

| # | 文件 | 改什么 | 漏掉的症状 |
|---|---|---|---|
| 1 | `src\types\project.ts:456-490` | 加到 `WorkflowNodeType` union | 类型不安全，靠 `as` 强转 |
| 2 | `src\components\workflow\nodes\XxxNode.tsx` | 新建节点组件 | 画布上渲染不出来 |
| 3 | `WorkflowEditor.tsx:66-98` | 加到 `nodeTypes` | ReactFlow 不认识 |
| 4 | `WorkflowEditor.tsx:100-123` **和** `:125-151` | **两张映射表都要加** | 当前 4 个节点就漏了 `rfTypeToWfType` |
| 5 | `NodePalette.tsx:39-107` | 加到分组（含 i18n key，**禁止硬编码中文**） | 用户不可达（`split` 就是这个问题） |
| 6 | `engine.ts` switch | 加 case + **补 default 分支** | 静默返回空，下游误报"缺输入" |

**额外要求**：
- i18n 三语同步（zh/en/ja），`NodePalette.tsx:44-58` 目前 AI/效果类节点硬编码中文，song 节点族才走 i18n，需统一
- 损伤等级标注（在节点组件里显示 🟢🟡🟠🔴 图标）

### 7.8 静默失败治理（修 1.4 节的 bug）

| 问题 | 现状 | 修法 |
|---|---|---|
| engine switch 无 default | `:1393` 直接闭合 | 加 `default: throw new Error(\`NODE_TYPE_UNSUPPORTED: ${nodeType}\`)` |
| speedShift 静默降级 | `:963-972` 只 console.warn | 改为抛错，或置节点 `degraded` 状态 + toast warning |
| deepOriginal 全静默 | `:1114,1121` 空 catch | 同上 |
| NodeStatus 缺警告态 | `store\workflow.ts:3` 只 5 态 | 加 `degraded` / `bypassed`，配橙色（黄色是排队色，`NodeShell.css:195` 有规约注释） |

---

## 第 8 章 · 工作流方案配方库（什么节点配什么节点，出什么效果）

本章是全文最实用的一章。每条配方给出：**节点连线图 → 逐节点参数 → 音频域处理次数 → 累积损伤评级 → 原创度评估 → 适用场景**。

「音频域处理次数」是本章的核心指标。回看第 3 章的累积损伤公式，**音频域每多走一次非线性/重建型处理，前面所有努力都会被最差的那一环吃掉**。所以配方的排序原则就一句话：**音频域处理次数越少的配方越靠前**。

### 8.0 配方总览

| 配方 | 名称 | 音频域处理次数 | 累积损伤 | 原创度 | 平台风险 |
|---|---|---|---|---|---|
| **A** | 从零符号域创作 | 1（单次渲染） | 🟢 ~L1，等效无损 | ★★★★★ 100% | 无 |
| **B** | AI 起草 + 符号域重构 | 1 | 🟢 ~L1 | ★★★★☆ 60–85% | 低 |
| **C** | 纯伴奏重制（stem→MIDI→重渲） | 1（新渲染，旧音频丢弃） | 🟢 ~L1 | ★★★★☆ 70–90% | 低 |
| **D** | 人声替换（保留原伴奏） | 2（伴奏分离 + 人声渲染） | 🟠 L4 主导 ~35dB | ★★☆☆☆ 30% | **高（伴奏未授权）** |
| **E** | 外部歌曲全扒复刻 | 1–2 | 🟡 视是否保留原音频 | ★★★☆☆ 50–75% | 中（旋律相似度） |
| **F** | 发行前母带 + 合规体检 | +1（可 L2 级） | 🟡 L2 ~110dB | — | — |
| **G** | 人声真实化增强（可选挂载） | +1 | 🟠 L3 ~70dB | — | — |

**一句话选路**：想要真原创又要音质，走 **A 或 C**；手上只有一首 AI 成品想改造，走 **B**；想留别人的伴奏，**别走 D**（法律风险远大于技术风险）。

### 8.1 配方 A · 从零符号域创作（音质天花板，原创度天花板）

**核心思想**：整条链路里音频只被"生成"一次，之前所有创作动作都发生在符号域（音符/和弦/歌词），符号域改动是 🟢L0 无损的。

```
                    ┌─────────────┐
                    │ chordBlockIn│  和弦进行手输（I-V-vi-IV 之类）
                    └──────┬──────┘
                           │ chords
                    ┌──────▼──────┐
                    │  melodyGen  │  🐛需重做（现 stub 每小节一个根音）
                    └──────┬──────┘
                           │ notes
        ┌──────────────────┼──────────────────┐
        │                  │                  │
  ┌─────▼─────┐     ┌──────▼──────┐   ┌───────▼───────┐
  │ harmonizer│     │  humanize   │   │ velocityCurve │  ❌需新增
  │  （配和声）│     │  ❌需新增    │   │  （力度曲线）  │
  └─────┬─────┘     └──────┬──────┘   └───────┬───────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │ notes（已人性化）
                    ┌──────▼──────┐
                    │  arranger   │  ✅已有（arranger.ts:118）
                    │ （多轨编曲） │
                    └──────┬──────┘
              ┌────────────┼────────────┐
              │            │            │
      ┌───────▼──────┐ ┌───▼────┐ ┌────▼─────┐
      │ soundfontRend│ │ 同上   │ │ 同上     │  每轨独立挂音源
      │ （钢琴/弦乐）│ │（贝斯）│ │ （鼓）   │  ← 唯一一次音频域生成 🟡L1
      └───────┬──────┘ └───┬────┘ └────┬─────┘
              └────────────┼────────────┘
                    ┌──────▼──────┐
                    │    merge    │  ❌需新增（混合成伴奏）
                    └──────┬──────┘
                           │ audio（伴奏）
                           │
   歌词 ──► lyricsIn ──► renderVocal ──┐   ✅已有（inference.rs:2004）
              （自己训练的声音模型）    │   ← 人声侧唯一一次生成 🟡L1
                                       │
                    ┌──────────────────▼──┐
                    │       merge         │  人声 + 伴奏
                    └──────┬──────────────┘
                           │
                    ┌──────▼──────────────┐
                    │  配方 F（母带+体检） │
                    └──────┬──────────────┘
                           │
                    ┌──────▼──────┐
                    │   output    │
                    └─────────────┘
```

**逐节点参数建议**

| 节点 | 关键参数 | 建议值 | 理由 |
|---|---|---|---|
| `chordBlockIn` | 和弦文本 | `C\|G\|Am\|F` 用 `\|` 分隔 | `engine.ts:1145` 用 `split(/[\|,;]/)` |
| `melodyGen` | style / mood / key | 按曲风 | 现版本是 stub，Phase 2 重做后才可用 |
| `humanize` | timing ±ms | 8–20ms | 超过 30ms 会听出拖拍 |
| `humanize` | velocity ±% | 8–15% | 太大像误触 |
| `velocityCurve` | 曲线类型 | 乐句渐强 | 机械等力度是"AI 味"最大来源 |
| `arranger` | swing | 0.15–0.35 | `arranger.ts:146-253` 已实现，`step%4===2` 右移 |
| `soundfontRender` | 采样率 | 44100 | 与 NSF-HiFiGAN 输出对齐（`nsf_hifigan.rs:32`），避免重采样 |
| `renderVocal` | transpose | 0 | 音域不合就改 MIDI 音符，别在音频域移调 |
| `renderVocal` | 模型 | 自己训练的 SoVITS | 版权与"真人感"双解 |

**音频域处理次数**：伴奏 1 次（soundfont 渲染）+ 人声 1 次（声码器）+ 母带 1 次（🟡L2）。

**累积损伤**：`merge` 是纯加法（🟢L0+），母带链走 L2 时总 SNR ≈ 110dB → **听感等效无损**。

**原创度**：★★★★★ 旋律自己生成、和声自己配、编曲自己排、音源本地渲染、人声自己的模型 → 每个采样点都是本地新算的，**波形里物理上不存在任何第三方内容**。

**适用场景**：真正想做原创发行的主路径。这是本文档推荐的默认路线。

**缺口清单（Phase 2 要补）**：`melodyGen` 重做、`humanize`、`velocityCurve`、`merge`。

### 8.2 配方 B · AI 起草 + 符号域重构（手上已有 AI 成品时的最优路线）

**核心思想**：把 AI 的产物当"草稿纸"用，只取它的**符号信息**（旋律轮廓、和弦、结构、歌词），**丢掉它的音频**。丢掉音频这一步就同时解决了三件事：声码器伪影没了、RVQ 残差没了、AI 元数据没了——不是"清除"，是**压根不生成那份文件**。

```
  ┌──────────────────────┐
  │ AI 成品音频 / YuE-2  │
  └──────────┬───────────┘
             │
     ┌───────┴────────┐
     │                │
┌────▼─────┐   ┌──────▼──────┐
│separation│   │ amtLyrics   │  ✅已有（amt_lyrics.rs，Whisper）
│ (MSST)   │   │ （歌词提取） │
└────┬─────┘   └──────┬──────┘
     │ stems           │ lyrics
     ├──────────┐      │
┌────▼────┐ ┌───▼────┐ │
│   amt   │ │chordDet│ │  ✅已有（amt.rs / chordAnalysis.ts）
│(→ MIDI) │ │(和弦)  │ │
└────┬────┘ └───┬────┘ │
     │ midi     │chords│
┌────▼─────────────────▼─┐
│  midiCleanup            │  ✅已有（midiCleanup.ts，7 类清理）
└────┬────────────────────┘
     │ notes
┌────▼──────────────────────────────────────┐
│  ★ 旋律重构（第 4.2 节五法）★             │
│  melodyRestructure  ❌需新增               │
│  推荐：法二节奏切分 + 法三轮廓保形 ≈ 60%   │
└────┬──────────────────────────────────────┘
     │ notes（已重构，与原曲差异 ≥55%）
┌────▼─────┐
│ humanize │ velocityCurve │ arranger  （同配方 A）
└────┬─────┘
     │
┌────▼──────────────────────────────────────┐
│  ★ 此处起完全等同配方 A 的下半段 ★        │
│  soundfontRender ×N → merge → renderVocal │
│  → merge → 配方 F → output                │
└───────────────────────────────────────────┘

     ✗ 原 AI 音频在 amt/chordDetect 之后被丢弃，不进入输出链
```

**关键决策点**：`amt` 之后**必须**过 `melodyRestructure`。如果直接拿扒出来的 MIDI 重渲，旋律 n-gram 与原曲 100% 一致，会被相似度检测（2.3 节）命中——这时你换了音源换了人声，但**旋律还是别人的**，法律上仍是改编而非原创。

**YuE-2 特例（更优）**：YuE-2 输出 ABC 乐谱，可**无损**转 MIDI（不需要 `amt` 声学转录，没有转录误差）。这条支路把入口从"音频"直接换成"符号"，音频域处理次数从 1 降到 0。

| 入口类型 | 转符号方式 | 符号精度 | 是否需要 separation |
|---|---|---|---|
| YuE-2（ABC 乐谱） | 直接解析 | 🟢 精确 | 否 |
| ACE-Step（只有音频） | `separation` + `amt` | 🟡 有转录误差 | 是 |
| 外部 MP3 | 同上 | 🟡 同上 | 是 |

**音频域处理次数**：输出链上 1 次（重渲）。分离和转录虽然跑了 DSP，但**它们的音频输出被丢弃**，只取符号，所以不计入损伤链。这是本配方最重要的性质。

**累积损伤**：与配方 A 同级 🟢 ~L1。

**原创度**：★★★★☆ 60–85%，取决于 `melodyRestructure` 的强度设置。**和声进行不受版权保护**（4.3 节），所以扒来的和弦可以放心直接用。

**适用场景**：用 ACE-Step / YuE-2 出了个满意的 demo，想把它做成能发行的原创作品。

**缺口清单**：`melodyRestructure`（Phase 2）、YuE-2 ABC 解析入口（Phase 2）。

### 8.3 配方 C · 纯伴奏重制（stem 分离 → MIDI → 本地音源重渲）

**核心思想**：伴奏是最容易"洗干净"的部分。因为伴奏几乎全是**确定音高的乐器音**，转录成 MIDI 的精度远高于人声，而 MIDI 一旦到手，重渲之后原始波形一个采样点都不剩。这是"别改波形，重新演奏"最典型的落地形态。

```
  ┌──────────────┐
  │  原曲音频    │
  └──────┬───────┘
         │
  ┌──────▼──────────────────────────────────┐
  │  separation（MSST）                     │  ✅已有
  │  模型：htdemucs_6s（6 stem）             │  ⚠️ stem 顺序见 1.5 节
  │  Drums/Bass/Other/Vocals/Guitar/Piano   │
  │  numOverlap 4 / shifts 2 / normalize ✓  │
  └──┬────┬────┬─────┬──────┬──────┬────────┘
     │    │    │     │      │      │
   drums bass other vocals guitar piano
     │    │    │     │      │      │
     │    │    │     ✗丢弃  │      │        ← 人声不要，配方 D 才要
     │    │    │            │      │
  ┌──▼──┐ │  ┌─▼──┐      ┌─▼──┐ ┌─▼──┐
  │drum │ │  │amt │      │amt │ │amt │
  │Detect│ │  └─┬──┘      └─┬──┘ └─┬──┘   ✅ drumDetection.ts / amt.rs
  └──┬──┘ │    │            │      │
     │  ┌─▼──┐ │            │      │
     │  │amt │ │            │      │
     │  └─┬──┘ │            │      │
     └────┴────┴────────────┴──────┘
                    │ 各轨 notes
          ┌─────────▼──────────┐
          │   midiCleanup      │  ✅已有
          └─────────┬──────────┘
                    │
          ┌─────────▼──────────────────────────┐
          │  ★ 可选：编曲改造（提升原创度）★    │
          │  · reharmonize     ❌需新增         │
          │  · rhythmVariation ❌需新增         │
          │  · addInstrument   （加新声部）     │
          │  · structureEdit   （改段落结构）   │
          └─────────┬──────────────────────────┘
                    │
          ┌─────────▼──────────┐
          │  humanize          │  ❌需新增（关键！见下文）
          │  velocityCurve     │
          └─────────┬──────────┘
                    │ notes（人性化）
     ┌──────────────┼──────────────┬─────────────┐
     │              │              │             │
┌────▼─────┐ ┌──────▼─────┐ ┌──────▼─────┐ ┌─────▼──────┐
│soundfont │ │ soundfont  │ │ soundfont  │ │ soundfont  │
│ Salamander│ │ Karoryfer  │ │ VSCO CE    │ │ SM Drums   │  ← 每轨挂不同音源
│ (钢琴)    │ │ (贝斯)     │ │ (弦乐)     │ │ (鼓)       │
└────┬─────┘ └──────┬─────┘ └──────┬─────┘ └─────┬──────┘
     └──────────────┼──────────────┴─────────────┘
              ┌─────▼──────┐
              │   merge    │  ❌需新增
              └─────┬──────┘
                    │ 全新伴奏音频
              ┌─────▼──────────────┐
              │ 接配方 A 的人声侧   │
              │ renderVocal → merge │
              └─────┬──────────────┘
                    │
              ┌─────▼──────┐
              │  配方 F     │ → output
              └────────────┘
```

**为什么 `humanize` 在这条链上是决定性的**

`amt` 转录出来的 MIDI 有个致命特征：**音符起始被量化对齐了**。原曲里鼓手打的是 `-7ms / +12ms / -3ms` 的人味偏移，转录器把它们统统吸到网格上。你拿这份"死板 MIDI"重渲，听起来就是 MIDI 味，不是因为音源差，是因为**时间轴被杀死了**。所以：

| 轨道 | timing 抖动建议 | velocity 抖动 | 说明 |
|---|---|---|---|
| 鼓（kick/snare） | ±5–12ms | ±10% | 底鼓稳一点，军鼓可松 |
| 鼓（hihat/shaker） | ±10–20ms | ±15% | 高频打击最需要松动 |
| 贝斯 | ±8–15ms | ±8% | 贝斯偏晚一点会更"沉" |
| 钢琴/吉他 | ±10–18ms | ±12% | 和弦内部各音再各自微错开 3–8ms |
| 弦乐 | ±15–30ms | ±10% | 弓弦起振本来就慢，抖大点更真 |

**再叠一层**：`arranger.ts:146-253` 已有的 swing 在这里可以直接用，`swing 0.12–0.2` 的轻微摆动能让"网格感"消失。

**音源分层是另一个决定性因素**（现在 1 个节点只能挂 1 个 preset，这是 1.3 节确认的缺口）。真实钢琴录音是多个麦克风位置叠出来的，单层采样一定单薄。Phase 3 的 `多音源分层` 让一个 notes 输入同时喂 2–3 个音源再混合：

```
notes ──┬──► soundfont(Salamander, gain 0dB, pan 0) ──┐
        ├──► soundfont(Black&Green, gain -9dB, pan 0)─┼──► merge ──► 更厚的钢琴
        └──► soundfont(FluidR3 GM, gain -15dB)────────┘
```

**音频域处理次数**：1 次（新渲染）。原伴奏音频在 `amt` 之后被丢弃。

**累积损伤**：🟢 ~L1。注意 `separation` 本身是 🔴L4 重建，但**它的输出不进最终链**，所以 L4 的损伤不计入。这正是配方 C 优于配方 D 的根本原因。

**原创度**：★★★★☆ 70–90%。若加了 `reharmonize` + `addInstrument` + `structureEdit`，可以做到"同一首歌听得出血缘，但每个声部都是自己重新演奏的"。

**适用场景**：喜欢某个编曲的思路，想做成自己的版本；或想把自己以前的作品换成更好的音源重制。

**注意**：本配方**不解决旋律版权**。如果主旋律照抄，仍会被 2.3 节的相似度检测命中。要彻底原创必须叠 `melodyRestructure`（配方 B 那一步）。

**缺口清单**：`merge`、`humanize`、`velocityCurve`、多音源分层（Phase 3）、`reharmonize`/`rhythmVariation`/`structureEdit`（Phase 2）。

### 8.4 配方 D · 人声替换（保留原伴奏）——技术可行，但我必须先说风险

**先说结论**：这条链**技术上最简单，法律上最危险，音质上最差**。三个"最"全踩在一起，所以我把它排在后面并给它挂红标。

**法律风险（必须先看）**：保留别人的伴奏音频进入你的输出文件，等于把第三方录音制品母带带进了你的发行物。这会被 2.1 节的**版权指纹**（Content ID / Audible Magic / Pex）直接命中——注意这类系统的检出率是 ★★★★★，不是 AI 检测那种 ★★☆☆☆。后果不是"标个 AI 标签"，而是下架、分成划走、账号处罚。**第 0 章铁律二说的就是这件事：任何作用在已生成波形上的处理都无法解决版权指纹。** 调速、变调、加噪、QPA/SDF/SFR 全都不行。

**唯一让这条链合法的前提**：伴奏是你自己的，或你拿到了明确授权，或它是 CC0/公有领域素材。满足这个前提，下面的链就可以放心用。

```
  ┌──────────────┐
  │  原曲音频    │  ← 必须是你有权使用的
  └──────┬───────┘
         │
  ┌──────▼───────────────────────────────────┐
  │  separation（MSST）                      │  🔴L4 ~35dB
  │  推荐模型：mel_band_roformer 系（人声档） │
  │  或 karaoke 类模型（保留伴奏更完整）      │
  │  numOverlap 6–8 / useTta ✓ / shifts 2    │  ← 这里别省算力
  └──────┬────────────────────┬──────────────┘
         │ instrumental       │ vocals
         │                    │
         │            ┌───────▼────────┐
         │            │ amtLyrics      │  取歌词
         │            │ f0 提取        │  取音高曲线
         │            └───────┬────────┘
         │                    │
         │            ┌───────▼──────────────────┐
         │            │  路线 D1：renderVocal    │  用自己的模型重唱
         │            │  （推荐，人声全新生成）   │
         │            │  路线 D2：RVC 换声       │  在原人声上换音色
         │            │  （🟠L4，保留原演唱表情） │
         │            └───────┬──────────────────┘
         │                    │ 新人声
  ┌──────▼────────────────────▼──────┐
  │            merge                 │  🟢L0+ 纯加法
  └──────┬───────────────────────────┘
         │
  ┌──────▼──────────────┐
  │  配方 F（母带+体检） │ → output
  └─────────────────────┘
```

**D1 vs D2 选择表**

| | D1 renderVocal（重唱） | D2 RVC 换声 |
|---|---|---|
| 输入需要 | 歌词 + MIDI 音符 | 原人声音频 |
| 原人声波形是否进输出 | ❌ 不进（全新生成） | ✅ 进（被变换过） |
| 音质 | 🟡L1 单次渲染 | 🟠L4（分离 + 换声两次重建） |
| 演唱表情 | 需自己做（`autotune.rs` 表情参数） | 继承原演唱，最省事 |
| 人声版权 | 🟢 干净（你的模型 + 你的音符） | 🟡 音色是你的，**演唱表演仍是原唱的** |
| 累积 SNR | ~L1 | 分离 35dB + 换声 → **总 ~32dB** |
| 推荐度 | ★★★★★ | ★★☆☆☆ |

**D2 的隐蔽问题**：很多人以为"RVC 换了音色就是我的声音了"。不对。RVC 只换音色（timbre），**演唱的时间、音高走向、颤音、气口全部是原唱的表演**，这在法律上是邻接权范畴。而且 D2 的输入是分离出来的人声——已经带了 35dB 的分离伪影，换声再重建一次，最终输出的人声底噪和"水声感"会很明显。

**如果一定要走 D2，参数这样给**（基于 `inference.rs` 实证）：

| 参数 | 位置 | 建议值 | 说明 |
|---|---|---|---|
| `transpose` | `:1697` | 0 | 非零就要相位声码器介入，再掉一档 |
| `protect` | `:1745` | 0.33 | 保护清音/辅音不被过度转换，太低会"糊字" |
| `rms_mix_rate` | `:1762` | 0.25 | 混回原始包络，保留原唱动态；1.0 = 完全用原包络 |
| `f0` 阈值 | `f0.rs:30` | RVC 0.03 | 已调好，别动 |
| 分离 `useTta` | SeparationNode | ✓ 开 | 分离质量是这条链的瓶颈，TTA 值得那点时间 |

**音频域处理次数**：D1 = 2（分离 + 人声渲染）；D2 = 3（分离 + 换声 + 母带）。

**累积损伤**：🟠 L4 主导，总 SNR ≈ 32–35dB。按第 3 章公式，你在后面做的任何 L2 级精细母带（110dB）都被这个 35dB 完全支配——**这就是为什么本配方排在后面**。

**原创度**：D1 ★★☆☆☆（伴奏和旋律都不是你的）；D2 ★☆☆☆☆。

**适用场景**：翻唱（走正规翻唱授权）、自己旧作品换人声、CC0 伴奏配唱。**不适用于宣称原创发行**。

**缺口清单**：`merge`（Phase 1）。其余全部已有。

### 8.5 配方 E · 外部歌曲全扒复刻（伴奏 + 人声都重做）

**核心思想**：配方 C（伴奏重制）+ 配方 B（旋律重构）+ 配方 A 的人声侧，三者合体。这是"扒带 → 复刻 → 原创"这条用户点名路径的完整形态。

```
  原曲音频
     │
  ┌──▼──────────┐
  │ separation  │ ──► vocals ──┬─► amtLyrics ────────► lyrics
  │  6 stem     │              ├─► f0 提取 ──────────► 参考音高曲线
  └──┬──────────┘              └─► amt（人声转 MIDI）─► 主旋律 notes
     │                                                      │
     └─► drums/bass/other/guitar/piano                       │
              │                                              │
         ┌────▼─────┐                              ┌─────────▼──────────┐
         │ amt ×N   │                              │ melodyRestructure  │ ❌需新增
         │ drumDet  │                              │ 强度 55–75%        │
         └────┬─────┘                              └─────────┬──────────┘
              │ 各轨 notes                                    │ 全新旋律
         ┌────▼────────────┐                                  │
         │ chordDetect     │ ──► chords（🟢可直接用，和声无版权）│
         └────┬────────────┘                                  │
              │                                               │
         ┌────▼─────────────────────────┐                     │
         │ reharmonize（可选，改和声色彩）│                     │
         │ rhythmVariation（改节奏型）   │                     │
         │ addInstrument（加新声部）     │                     │
         │ structureEdit（改段落）       │                     │
         └────┬─────────────────────────┘                     │
              │                                               │
         ┌────▼──────────────┐                                │
         │ humanize          │                                │
         │ velocityCurve     │                                │
         └────┬──────────────┘                                │
              │                                               │
      ┌───────▼────────────────────┐                          │
      │ soundfontRender ×N（分层） │                          │
      └───────┬────────────────────┘                          │
              │                                               │
         ┌────▼────┐                          ┌───────────────▼──────┐
         │  merge  │ 新伴奏                    │ renderVocal          │
         └────┬────┘                          │ （自己的声音模型）    │
              │                               │ + lyrics + 新旋律     │
              │                               └───────────┬──────────┘
              └────────────┬──────────────────────────────┘
                     ┌─────▼─────┐
                     │   merge   │
                     └─────┬─────┘
                     ┌─────▼──────────────┐
                     │ 配方 F（母带+体检）│ → output
                     └────────────────────┘

  ✗ 原曲的所有音频 stem 在转录后全部丢弃，不进输出链
```

**原创度分档表**（取决于你在"编曲改造"和 `melodyRestructure` 上下多少功夫）

| 改造强度 | melodyRestructure | 编曲改造 | 原创度 | 相似度检测风险 |
|---|---|---|---|---|
| 最轻（纯换音源） | 关 | 无 | ★★☆☆☆ 30% | 🔴 高（旋律 100% 同） |
| 轻 | 法一 15–25% | 换音源 | ★★★☆☆ 45% | 🟠 中高 |
| **中（推荐）** | **法二+法三 ≈60%** | **reharmonize + humanize** | **★★★★☆ 70%** | **🟡 低** |
| 重 | 法四 55–75% | + addInstrument + structureEdit | ★★★★☆ 85% | 🟢 很低 |
| 极重 | 法五 ~100% | 全部 | ★★★★★ 95%+ | 🟢 无 |

到了"极重"档其实已经不叫复刻了，那就是配方 A——**只是借用了原曲的和声框架和段落感做灵感**。这是完全正当的创作方式，音乐史上一直如此。

**音频域处理次数**：1（重渲）。分离与转录的音频输出全丢弃。

**累积损伤**：🟢 ~L1 + 母带 L2 → 总 ≈ 110dB，**听感无损**。

对比一下市面上"一键转原创"的做法：调速 + 变调 + 加噪 + 削波，输出大概在 🟠L3–🔴L4 之间（60–90dB 甚至更差），**而且对版权指纹零效果**。本配方音质好一个数量级，原创度是真的，风险还更低。这就是第 0 章铁律三的实证。

**适用场景**：这是用户需求里"AI 制作的歌曲扒带 → 复刻 → 伴奏解析 MIDI → 转换音源 → 主唱克隆 → 完成原创歌曲"的标准答案链路。

**缺口清单**：`melodyRestructure`、`reharmonize`、`rhythmVariation`、`structureEdit`、`humanize`、`velocityCurve`、`merge`、多音源分层。

### 8.6 配方 F · 发行前母带 + 合规体检（所有配方的公共尾段）

**核心思想**：这一段是所有配方共用的"出厂检验线"。它的设计原则和别的段落不一样——**这里每一个节点都必须能说清自己的损伤等级，能关，且默认参数保守**。母带不是"让声音变响"，是"让声音在任何播放设备上都成立"。

```
  混音成品（人声 + 伴奏 merge 后）
         │
  ┌──────▼──────────────────┐
  │ ① dcOffsetRemove        │  🟢L0+  ❌需新增（6.3 节）
  │   高通 5Hz / 或均值扣除  │  先扣直流，否则后面所有峰值测量都偏
  └──────┬──────────────────┘
         │
  ┌──────▼──────────────────┐
  │ ② busEq（多段EQ）       │  🟡L2 ~115dB  ❌需新增（6.7 节）
  │   仅做减法，别做加法     │  推荐：只切不提，宁少勿多
  └──────┬──────────────────┘
         │
  ┌──────▼──────────────────┐
  │ ③ stereoWidth           │  🟡L2  ❌需新增（6.7 节）
  │   M/S 矩阵，width 0.9–1.1│  超过 1.3 单声道折叠会丢中频
  └──────┬──────────────────┘
         │
  ┌──────▼──────────────────┐
  │ ④ saturate（可选）      │  🟠L3 ~70dB  ❌需新增
  │   tanh 软饱和 drive ≤10% │  这是艺术染色，不是"无损处理"
  └──────┬──────────────────┘   想要最高音质就 bypass 掉
         │
  ┌──────▼──────────────────┐
  │ ⑤ mastering（已有）     │  🟠L3（含压缩）✅ mastering.ts:103
  │   EQ+压缩+限幅三件套     │  阈值 -14dB / 2.5:1 / 天花板 -1dBFS
  └──────┬──────────────────┘
         │
  ┌──────▼──────────────────┐
  │ ⑥ lufsNormalize         │  🟢L0+ >140dB  ❌需新增（6.1 节）
  │   target -14 LUFS        │  纯标量增益，这一步真·无损
  │   truePeak -1.0 dBTP     │
  └──────┬──────────────────┘
         │
  ┌──────▼──────────────────┐
  │ ⑦ dither（仅降位深时）  │  🟢 唯一合法加噪（3.7 节）
  │   TPDF + 噪声整形        │  24bit→16bit 才需要；导 32f 不要开
  └──────┬──────────────────┘
         │
  ┌──────▼──────────────────────────────────┐
  │ ⑧ complianceCheck（只读，无损伤）       │  ❌需新增（6.8 节）
  │   LUFS / truePeak / 削波 / DC /         │
  │   相位相关性 / 静音头尾 → JSON 报告      │
  └──────┬──────────────────────────────────┘
         │
  ┌──────▼──────┐   ┌────────────────┐
  │   output    │   │ spectrogram    │ 旁挂，人眼复检
  └─────────────┘   │ phaseCorrelation│
                    │ spectralCompare │ 与母带前 A/B 对比
                    └────────────────┘
```

**顺序为什么是这个顺序**（这是母带工程的硬性因果，不能随便调）

| 位置 | 为什么必须在这 |
|---|---|
| dcOffset 最前 | 直流偏移会吃掉正向余量，不先扣，后面限幅器会误判峰值 |
| EQ 在压缩前 | 先修频率平衡，压缩器才能对"已经正确"的信号做动态判断 |
| stereoWidth 在压缩前 | 加宽会改变侧信号能量，压缩后再加宽会破坏已建立的动态关系 |
| 饱和在限幅前 | 饱和本身产生谐波会抬峰值，必须让限幅器兜底 |
| **LUFS 规整在限幅之后** | 这是最关键的一条。限幅器决定了峰值天花板，LUFS 只做整体增益缩放。反过来做会让限幅器二次工作，多一层非线性 |
| dither 绝对最后 | dither 的意义就是掩盖"最后一次量化"的误差。它后面再做任何处理，dither 噪声会被处理放大，白做 |
| complianceCheck 最末 | 检验的必须是最终成品 |

**三档预设建议**（给用户直接选，别让用户逐个调）

| 预设 | 启用节点 | 累积 SNR | 适用 |
|---|---|---|---|
| 🟢 **透明档** | ① + ⑥ + ⑦ + ⑧ | **>130dB，实质无损** | 混音本身已经很好，只需规整响度 |
| 🟡 **标准档（默认）** | ① + ② + ③ + ⑥ + ⑦ + ⑧ | ~112dB，听感无损 | 大多数情况 |
| 🟠 **响度档** | 全开（含 ④⑤） | ~68dB | 需要竞争性响度的流行/电子 |

注意 🟠 响度档的 68dB——这已经掉到"仪器明确可测"的水平了。但它的损伤来自压缩和饱和，这是**音乐性的选择**（你要那个"胶水感"和"厚度"），和"为了骗检测而损伤"完全是两件事。**用在该用的地方，损伤是艺术；用在骗检测上，损伤纯是浪费。**

**各平台响度目标表**（`lufsNormalize` 的 target 参考）

| 平台 | 目标 LUFS | true peak 上限 | 备注 |
|---|---|---|---|
| Spotify / Apple Music | -14 | -1.0 dBTP | 超了会被平台压回去，不如自己控 |
| YouTube | -14 | -1.0 dBTP | 同上 |
| 抖音 / 快手 | -14 ~ -12 | -1.0 dBTP | 移动端偏好稍响 |
| 网易云 / QQ音乐 | -14 | -1.0 dBTP | |
| CD / 母带交付 | -9 ~ -12 | -0.3 dBTP | 无平台归一化 |
| **广播（EBU R128）** | **-23** | -1.0 dBTP | 严格标准 |

**为什么 truePeak 要 -1.0 而不是 -0.1**：截图里那个"输出限制 -0.10 dB"是典型的过于乐观。转成有损编码（AAC/MP3/Opus）后，编解码器的重建波形峰值会**超过**原始 PCM 峰值，实测常见 +0.5 到 +1.5 dB 的 overshoot。留 -1.0 dBTP 是给编码器留的余量。留 -0.1 的结果是在用户手机上播放时产生 inter-sample clipping，这是听得出来的。而且 true peak 必须用 **4× 过采样**测（6.1 节），直接测 PCM 样本峰值会低估。

**缺口清单**：`dcOffsetRemove`、`busEq`、`stereoWidth`、`saturate`、`lufsNormalize`、`dither`、`complianceCheck`、`spectrogram`、`phaseCorrelation`、`spectralCompare`（Phase 1 + Phase 4 + Phase 5）。

### 8.7 配方 G · 人声真实化增强（可选挂载模块）

**核心思想**：第 5 章讲过，人声真实感是**连续谱**，工程当前已经在"接近真"这一档。这个模块的目的是**补充真实成分**，不是"破坏 AI 特征"——这两个说法听着像，做法完全相反：前者往里加真实世界本来就有的东西，后者是往里加噪声破坏。前者音质变好，后者音质变差。

```
  renderVocal 输出的人声
         │
  ┌──────▼──────────────────────────┐
  │ ① voiceRealism（高频激励混合）  │  🟠L3 ~72dB  ❌需新增
  │   crossover 8kHz / mix 5–15%    │  补齐声码器丢失的齿音空气感
  └──────┬──────────────────────────┘
         │
  ┌──────▼──────────────────────────┐
  │ ② formantJitter（共振峰微扰）   │  🟠L3 ~68dB  ❌需新增
  │   ±3–8% / 全通滤波器实现        │  真人共振峰不是恒定的
  │   速率 0.3–1.2 Hz               │  太快会像颤音，太慢没效果
  └──────┬──────────────────────────┘
         │
  ┌──────▼──────────────────────────┐
  │ ③ breathLayer（气声层，可选）   │  🟢L0+ 纯加法  ❌需新增
  │   在乐句起始/结束叠入气息采样    │  这是唯一无损的真实化手段
  └──────┬──────────────────────────┘
         │
  ┌──────▼──────────────────────────┐
  │ ④ harmonicityCheck（客观评分）  │  只读 ⚠️模块已存在未接线
  │   harmonic_energy_fraction_db   │  harmonicity.rs:50
  │   comb_depth_db / shimmer_db    │  :220 / :432
  └──────┬──────────────────────────┘
         │
    进 merge → 配方 F
```

**推荐用法：先测，再决定要不要处理**

这是我对这个模块最重要的建议。`harmonicity.rs` 里那三个指标已经写好了，只是没接到 UI 上。正确的工作流是**先跑 ④ 看数值，只在数值确实差的时候才开 ①②**：

| 指标 | 含义 | 健康范围 | 偏离时的处理 |
|---|---|---|---|
| `harmonic_energy_fraction_db` | 谐波能量占比 | 高 = 音高稳定清晰 | 太低说明 F0 提取出问题，回去查 f0，别在音频域补救 |
| `comb_depth_db` | 梳状伪影深度 | 越低越好 | 偏高说明声码器或分离有相位问题，**开 ① 高频激励能掩盖一部分** |
| `shimmer_db` | 幅度周期性抖动 | 真人 0.2–0.5dB | 太低（<0.1）= 过于机械 → 开 ② formantJitter |

**这个"先测后处理"的思路适用于整个规划**。市面工具的做法是无条件全套处理拉满，结果是音质白掉。正确做法是**测出问题在哪，只处理那个问题**。这也是为什么第 7.4 节的分析节点在本规划里权重很高——**分析节点零损伤，是纯收益**。

**②的实现要点（自己做，无现成开源）**：共振峰微扰不要用重采样或相位声码器（那是 L4）。用**级联全通滤波器**做频率相关的相位/群延迟扰动，配合一个极慢的 LFO 调制全通系数。这是 L3 而非 L4，因为它是时变的但不做时频域重建。

```
y[n] = -a·x[n] + x[n-1] + a·y[n-1]        // 一阶全通
a = a0 + depth · sin(2π·f_lfo·n/fs)       // a0 ≈ 0.6, depth ≈ 0.05
```

级联 4–6 级，每级 `a0` 错开，对应不同共振峰区域。

**③ breathLayer 的价值被严重低估**。它是纯加法（🟢L0+，不改原波形一个采样点），但对"真人感"的贡献可能超过 ①②相加。原因很简单：**真人唱歌一定要换气**，AI 生成的人声最典型的破绽就是"一口气唱完整段还不喘"。做法是准备一组自己录的气息采样（吸气/呼气/唇齿声），在乐句边界按 notes 的间隙自动插入。这个功能在符号域就能规划好插入点（用 `notes` 的 gap 检测），音频域只做加法。

**音频域处理次数**：①②各 1 次；③是 0 次（加法）；④是 0 次（只读）。

**累积损伤**：只开 ③④ → 🟢 无损；开 ①② → 🟠 ~66dB。

**推荐配置**：默认只开 ③ + ④。①② 留给"确实听出机械感"的场合，且强度取下限（① mix 5%，② depth ±3%）。

**缺口清单**：`voiceRealism`、`formantJitter`、`breathLayer`、`harmonicityCheck` 接线（Phase 7）。

### 8.8 节点搭配速查（什么配什么出什么效果）

上面 7 条配方是"整条链"。这一节是"两三个节点的局部组合"，用来现场拼装。

**✅ 推荐组合**

| 组合 | 出来的效果 | 损伤 |
|---|---|---|
| `amt` → `midiCleanup` → `humanize` → `soundfontRender` | 死板转录 MIDI 变成有人味的真实演奏 | 🟢L1 |
| `notes` → `soundfontRender` ×3（不同音源）→ `merge` | 单薄采样变厚实分层音色 | 🟢L1 |
| `chordDetect` → `reharmonize` → `arranger` | 保留原曲情绪骨架，和声色彩全换 | 🟢L0 |
| `melodyRestructure` → `harmonizer` → `arranger` | 新旋律自动配和声再自动编曲 | 🟢L0 |
| `arranger`(swing 0.15) + `humanize`(±12ms) | 双层时间松动，网格感彻底消失 | 🟢L0 |
| `renderVocal` → `breathLayer` → `merge` | 人声有了呼吸，最省音质的真实化 | 🟢L0+ |
| `harmonicityCheck` → 看数值 → 决定是否 `voiceRealism` | 只在真有问题时才处理 | 只读 |
| `lufsNormalize` → `dither` → `complianceCheck` | 发行级响度合规三连 | 🟢 |
| `spectralCompare`（母带前 vs 后） | 肉眼验证母带到底改了什么 | 只读 |
| `separation` → `amt` → ✗丢弃音频 | **本规划的核心手法**：只取符号，不带波形 | 🟢 |

**❌ 反面组合（会把音质白白搞坏）**

| 组合 | 为什么错 |
|---|---|
| `transposeAudio` 而不是改 MIDI 音符 | 音频移调 🔴L4，符号域移调 🟢L0，差 4 级 |
| `speedShift` 后再 `transposeAudio` | 两次相位声码器串联，SNR 掉到 30dB 以下 |
| `deepOriginal`（调速+变调）用于"防检测" | 对版权指纹零效果，对 AI 检测未证实，音质确定损失 |
| `dither` 之后再做任何处理 | dither 噪声被后续处理放大，等于白加噪 |
| `lufsNormalize` 在 `mastering` 之前 | 限幅器要二次工作，多一层非线性 |
| `saturate` + `mastering`(压缩) + 响度档全开 | 三层非线性叠加，~60dB，"数字疲劳感" |
| 导出 32bit float 还开 `dither` | 浮点不需要 dither，纯加噪 |
| 加超声/白噪/次声"干扰音频" | 3.6 节论证：零收益，纯抬噪声底 |
| 硬削波（用户说的"肖波"） | THD 1–10% 奇次谐波失真，损伤大于任何 L2 处理 |
| `separation` 输出直接进 `output` | 带 35dB 分离伪影，且原始波形版权仍在 |

**🔍 诊断组合（零损伤，随手挂）**

| 组合 | 用途 |
|---|---|
| 任意节点 → `spectrogram` | 看有没有奇怪的频谱空洞/横条纹伪影 |
| 任意节点 → `phaseCorrelation` | 看单声道折叠后会不会丢东西 |
| `output` 前 → `complianceCheck` | 发行前最后一道闸 |
| A 路 + B 路 → `abCompare` | 听感盲测某个节点到底有没有用 |
| `renderVocal` → `f0Curve` | 看音高曲线有没有跳跃/断裂 |
| `renderVocal` → `timbreMetrics` | MFCC/质心对比真人参考，量化"像不像" |

---

## 第 9 章 · 实施路线（分阶段，每阶段独立可验收）

排序原则：**先修已知 bug（改动小、收益确定），再补合规基础设施（发行必需），再补符号域（原创度核心），最后才是音频域处理**。这个顺序和第 0 章铁律三一致——把资源投在符号域和分析上，而不是投在音频域"魔法处理"上。

每个 Phase 结束的验收标准统一是：`cargo check` 通过 + `tsc -b && vite build` exit 0 + `npx vitest run` 全绿 + i18n 三语 parity。

### Phase 0 · 修已知 bug（最小改动，最高确定性）

| # | 问题 | 位置 | 修法 | 验收 |
|---|---|---|---|---|
| 0-1 | `rfTypeToWfType` 漏 4 个节点类型 | `WorkflowEditor.tsx:100-123` | 补齐 4 项，与 `wfTypeToRfType`（`:125-151`）对称 | 加单测断言两张表互为逆映射 |
| 0-2 | engine switch 无 default | `engine.ts:1393` | `default: throw new Error(\`NODE_TYPE_UNSUPPORTED: ${nodeType}\`)` | 单测传未知类型断言抛错 |
| 0-3 | `speedShift` 静默降级 | `engine.ts:963-972` | 改为置节点 `degraded` + toast warning | 单测断言状态变更 |
| 0-4 | `deepOriginal` 两个空 catch | `engine.ts:1114,1121` | 同 0-3 | 同上 |
| 0-5 | `split` 用户不可达 | `NodePalette.tsx:39-107` | 加进面板（i18n key，禁硬编码） | 手动确认面板可见 |
| 0-6 | `songGen` 声明输出含 midi 但实际没有 | `song-tasks.ts:55,57` | 改声明或补实现，二选一 | 单测断言声明与实际一致 |
| 0-7 | 离线导出主音量恒为 1 | `effectsBus.ts:104` | 确认是否有意为之；若非，接上项目主音量 | A/B 对比在线与离线导出电平 |
| 0-8 | `NodeStatus` 缺警告态 | `store\workflow.ts:3` | 加 `degraded` / `bypassed`，橙色（黄色是排队色） | 视觉确认 |
| 0-9 | `export.rs` 遗留未接线 | `src-tauri\src\audio\export.rs` | 确认无引用后删除，或补文档说明 | `cargo check` 无警告 |
| 0-10 | `NodePalette` AI/效果类硬编码中文 | `NodePalette.tsx:44-58` | 全部走 i18n | i18n parity 脚本通过 |

**Phase 0 的价值**：0-1 到 0-4 这四个是"静默失败"，用户遇到时会以为"功能没生效"而反复尝试，是最消耗信任的 bug 类型。

### Phase 1 · 合规体检基础设施（发行必需，全部零/极低损伤）

这是**最高优先级的新功能**。理由：没有 LUFS 测量，你根本不知道自己的成品能不能发行；而它本身是纯测量 + 纯标量增益，🟢 零损伤。**投入产出比全规划最高。**

| # | 交付 | 位置 | 依赖 |
|---|---|---|---|
| 1-1 | `measure_lufs` / `normalize_to_lufs` Rust 命令 | 新建 `src-tauri\src\commands\loudness.rs` | `ebur128` crate |
| 1-2 | true peak 4× 过采样测量 | 同上 | 无 |
| 1-3 | `dither`（TPDF + 噪声整形） | `utai-dsp` 新模块 | 无 |
| 1-4 | `dcOffsetRemove` | `utai-dsp` | 无 |
| 1-5 | `clipDetect`（削波区间定位） | `utai-dsp` | 无 |
| 1-6 | `phaseCorrelation` | `utai-dsp` | 无 |
| 1-7 | 静音头尾检测/修剪 | `utai-dsp` | 无 |
| 1-8 | `complianceCheck` 节点（JSON 报告） | 前端节点 + 后端聚合命令 | 1-1~1-7 |
| 1-9 | `merge` 节点（多路混合） | `engine.ts` + 节点组件 | 无 |
| 1-10 | `lufsNormalize` / `dither` 节点 | 同上 | 1-1, 1-3 |

**验收标准（除通用项外）**：
- 用已知 LUFS 的参考文件校验 `measure_lufs`，误差 < 0.1 LU
- `normalize_to_lufs` 输出与输入做逐样本比值，确认全曲**单一常数**（证明是纯增益）
- `dither` 输出与输入相减，确认残差是 TPDF 分布且能量 ≈ -110dBFS

**为什么 `merge` 放在 Phase 1**：7 条配方里有 6 条需要它。当前只能靠 Output 多 lane 变通，这是最大的结构性缺口。

### Phase 2 · 符号域节点（原创度的真正来源）

第 4 章论证过，符号域是唯一"改动大但损伤零"的战场。这个 Phase 决定了作品是不是真原创。

| # | 交付 | 依据章节 | 损伤 |
|---|---|---|---|
| 2-1 | `humanize`（timing/velocity 抖动，可按轨预设） | 4.4 | 🟢L0 |
| 2-2 | `velocityCurve`（乐句渐强/渐弱/自定义曲线） | 4.4 | 🟢L0 |
| 2-3 | `swingQuantize`（独立节点，复用 `arranger.ts:146-253` 逻辑） | 4.4 | 🟢L0 |
| 2-4 | `melodyRestructure` 五法 | 4.2 | 🟢L0 |
| 2-5 | `reharmonize`（和声重配） | 4.3 | 🟢L0 |
| 2-6 | `rhythmVariation`（节奏型变换） | 4.4 | 🟢L0 |
| 2-7 | `structureEdit`（段落结构编辑） | 4.5 | 🟢L0 |
| 2-8 | `melodyGen` 重做（替换 stub） | 4.2 | 🟢L0 |
| 2-9 | `harmonizer` bug 修复 | 7.1 | 🟢L0 |
| 2-10 | YuE-2 ABC 乐谱解析入口 | 8.2 | 🟢L0 |
| 2-11 | `breathLayer` 插入点检测（符号域部分） | 8.7 | 🟢L0 |

**`melodyRestructure` 的验收方式**（这是最需要客观验收的节点）：实现一个旋律相似度度量（n-gram Jaccard + 轮廓相关系数），单测断言"强度参数 60% 时，输出与输入的相似度落在 35–45% 区间"。**没有这个度量，"原创度 60%" 就只是一句话，不是工程指标。**

**2-1 的实现要点**：随机数必须**可复现**（种子化）。理由是用户调完参数试听满意后再导出，如果每次渲染抖动都不同，导出的和试听的不是同一个东西。参考 `effectsBus.ts:61-81` 里已有的 xorshift32 种子做法（seed `0x2f6e2b1`）。

### Phase 3 · 音源能力（"换音源"的质量上限在这里）

用户需求里"转换音源""多挂音源"落在这个 Phase。当前 SF2/SFZ 解析已经相当完整，但缺了几个关键表现力维度。

| # | 交付 | 现状依据 | 说明 |
|---|---|---|---|
| 3-1 | 多音源分层渲染 | 1.3 节缺口（一节点只能挂 1 preset） | 一份 notes 同时喂 N 个音源，各带 gain/pan，输出混合 |
| 3-2 | pitch bend 支持 | `synth.rs` 零命中 | 弯音是弦乐/吉他/管乐真实感的核心 |
| 3-3 | CC 控制器（CC1 modulation / CC11 expression / CC64 sustain） | `synth.rs` 零命中 | CC11 做长音渐强，弦乐立刻活了 |
| 3-4 | SF2 modulator 应用 | `sf2.rs:690,702` 只提取不应用 | 音源作者写好的调制关系当前全被忽略 |
| 3-5 | 低通滤波器（SF2 initialFilterFc/Q，SFZ cutoff/resonance） | `synth.rs` / `sfz.rs` 零命中 | 力度→亮度映射，最重要的"活"的来源 |
| 3-6 | SFZ `$变量` / xfin-xfout 交叉淡入 | `sfz.rs` 零命中 | 高质量 SFZ 库普遍用到，不支持会加载错 |
| 3-7 | 释放段指数曲线（当前全线性） | `synth.rs:588` | 线性释放听起来"断得很硬"，指数才自然 |
| 3-8 | 音源库下载条目（10 个开源库，见第 10 章） | `assets.rs:149-161` 已有 6 包机制 | 复用现有下载框架，加条目即可 |
| 3-9 | FluidSynth 通用化 | `amt_export.rs:296-363` 只服务 AMT | 作为 `soundfontRender` 的可选后端（对照组） |

**优先级内部排序**：3-1（分层）> 3-5（滤波器）> 3-3（CC11）> 3-2（pitch bend）> 其余。理由：3-1 和 3-5 对"听起来像真乐器"的贡献最大，而 3-4 SF2 modulator 虽然"正确"但很多免费音源根本没写 modulator，收益不确定。

**3-5 的实现要点**：SF2 的 `initialFilterFc` 单位是 absolute cents，`8.176 * 2^(cents/1200)` Hz。二阶低通用双二次（biquad）即可，Q 从 `initialFilterQ`（单位 cB）算 `Q = 10^(cB/200)`。**注意 SF2 规范里滤波器在包络之前**，顺序错了音色差别很明显。

**3-2 的注意点**：pitch bend 要走 `synth.rs:250-273` 那个 4 点三次 Hermite 插值的读取步进，不要另起一套重采样，否则和现有音质基线不一致。

### Phase 4 · 母带补全（配方 F 的落地）

| # | 交付 | 损伤 | 说明 |
|---|---|---|---|
| 4-1 | `busEq` 多段参量 EQ（≥5 段，含 shelf/peaking/HPF/LPF） | 🟡L2 | 复用 `mastering.ts:117-137` 的 biquad 思路，扩展成可配置段数 |
| 4-2 | `stereoWidth`（M/S 矩阵 + 可选去相关） | 🟡L2 | width 上限限制在 1.5，超过给警告 |
| 4-3 | `saturate`（tanh 软饱和 / 变压器风格） | 🟠L3 | 必须显式标注 L3，默认 drive 5% |
| 4-4 | 母带三档预设（透明/标准/响度） | — | 8.6 节那张表直接做成 UI 预设 |
| 4-5 | 全通相位旋转（可选，工具性质） | 🟡L2 | 只作为"峰值余量优化"工具，**不作为防检测手段**（3.4 节已鉴定） |
| 4-6 | 多段压缩（可选） | 🟠L3 | 优先级最低，单段够用 |

**4-5 的定位必须说清**：全通相位旋转（就是截图里那个 "QPA 量子相位对齐" 的真实身份）有一个**正当用途**——它能改变波形的峰值形状而几乎不改变听感，从而给限幅器多留 1–2dB 余量。这是母带工程里真实存在的技巧。但它**不能**用来对抗版权指纹，也**没有证据**能对抗 AI 检测。所以我把它做成工具，但在 UI 上明确写用途，不给"防检测"的暗示。

### Phase 5 · 分析可视化（零损伤，纯收益）

| # | 交付 | 现状 |
|---|---|---|
| 5-1 | `spectrogram` 离线时频图导出 | 1.3 节缺口，`utai-dsp\src\stft.rs` 已有 STFT 基础 |
| 5-2 | `f0Curve` 可视化 | 提取已有（`f0.rs:48-150`），只缺显示 |
| 5-3 | `timbreMetrics`（MFCC / 频谱质心 / 频谱滚降 / 过零率） | 全部零命中，需新增 |
| 5-4 | `harmonicityCheck` 接线 | 模块已存在（`harmonicity.rs:50,220,432`），只缺接线 |
| 5-5 | `spectralCompare`（A/B 频谱差异） | 需新增，依赖 5-1 |
| 5-6 | DTW 对齐（用于"改造前后"逐段比对） | 零命中，需新增 |
| 5-7 | `abCompare` 节点（听感盲测） | 需新增 |

**5-4 是最便宜的一项**：代码已经写好在 `harmonicity.rs` 里，接线工作量小，但它给出的是**人声质量的客观数值**，直接支撑 8.7 节"先测后处理"的工作方式。**建议在 Phase 5 里第一个做。**

**5-1 的实现建议**：直接用 `utai-dsp\src\stft.rs` 现成的 STFT，输出 PNG。注意 `vr.rs:197` 的 `librosa_stft` 是为模型推理服务的（center=True / periodic hann / f64 算 f32 存），可视化用不着那么严格，但**别混用**——分析用的 STFT 参数变了会让 5-5 的对比失去意义。

### Phase 6 · 工作流基础设施

| # | 交付 | 依据 |
|---|---|---|
| 6-1 | 端口类型系统（`PortDataType` / `PortSpec` / `NODE_PORTS`） | 7.6 节 |
| 6-2 | `isValidConnection` 接到 `<ReactFlow>` | `WorkflowEditor.tsx:1201-1212` 当前没传 |
| 6-3 | 节点 bypass 开关（`WorkflowNode` 加字段） | `project.ts:449-454` 只有 4 字段 |
| 6-4 | `degraded` / `bypassed` 状态与配色 | Phase 0-8 的延续 |
| 6-5 | 损伤等级图标显示在节点上（🟢🟡🟠🔴） | 本规划特有，让用户随时看到代价 |
| 6-6 | 4 个歌曲模板补 UI 入口 | 1.4 节 bug |
| 6-7 | 工作流预设"保存全部"（当前 `save_workflow_preset` 是单数） | `storage.rs:872-893` |

**6-5 是本规划最重要的 UI 设计主张**。市面工具把所有处理都包装成"一键优化"，用户看不到自己付出了什么代价。本工程应该反过来：**每个节点显示自己的损伤等级，链路末端显示累积 SNR 估算**。用户能看到"我这条链已经掉到 35dB 了"，才会自己去想"那个 L4 节点是不是能换掉"。

这个累积估算用第 3 章的公式即可：`总SNR(dB) ≈ -10·log10( Σ 10^(-SNR_i/10) )`，每个节点声明一个典型 SNR 值，引擎沿链累加。**不需要精确，只需要让用户感知量级。**

### Phase 7 · 人声真实化（可选，最后做）

| # | 交付 | 损伤 |
|---|---|---|
| 7-1 | `breathLayer`（气息层叠加 + 自带气息采样集） | 🟢L0+ |
| 7-2 | `voiceRealism`（高频激励混合） | 🟠L3 |
| 7-3 | `formantJitter`（级联全通 + 慢速 LFO） | 🟠L3 |
| 7-4 | WORLD 集成（可选，用于残差混合） | 🟠L3 |

**放最后的理由**：这个 Phase 的收益最主观（"听起来更真"因人而异），损伤最明确（L3），而且工程当前人声质量已经在"接近真"档。**先把前 6 个 Phase 做完，你的作品质量提升会比这个 Phase 大得多。**

**7-4 要谨慎**：`utai-dsp\src\formant.rs:1-12` 的注释明确写了 "WORLD-free path（WORLD was never actually integrated）"，说明原作者是**有意**避开 WORLD 的。引入前应先确认理由（可能是许可、体积或精度问题），别推翻原作者的判断。

### 阶段依赖图

```
Phase 0（修 bug）
   │
   ├──► Phase 1（合规体检 + merge）★最高优先
   │        │
   │        ├──► Phase 4（母带补全）
   │        └──► Phase 5（分析可视化）
   │
   ├──► Phase 2（符号域）★原创度核心
   │        │
   │        └──► Phase 3（音源能力）
   │
   └──► Phase 6（基础设施，可与其他并行）

              Phase 7（人声真实化，最后）
```

**如果只能做三件事**：Phase 0（修 bug）+ Phase 1（LUFS/dither/merge/体检）+ Phase 2 的 2-1/2-2/2-4（humanize / velocityCurve / melodyRestructure）。这三样做完，配方 A/B/C/E 就全部可跑，且是发行级质量。

---

## 第 10 章 · 开源引入清单（能抄的抄，没有的自己做）

用户要求"用得到的开源项目可以直接复制到工程；市面上找不到开源的，自己制作并说明详细方法"。本章按这个分法列。

**一条纪律先说**：下面所有条目的许可证在**实际引入前必须自己再核一遍上游仓库/官网**。开源项目改许可、换维护者的情况很常见，我这里标的是我所知的情况，**不能当作法律依据**。工程已经有一个前例可以参照——`assets.rs:143-147` 那个 training-vocoder 是 **CC BY-NC-SA 4.0**（非商用），原作者专门标了出来，说明这个工程对许可是认真的，别破坏这个纪律。

### 10.1 可直接引入的 Rust crate

| 用途 | crate | 用在哪 | 许可（需核验） |
|---|---|---|---|
| EBU R128 响度测量 | `ebur128` | Phase 1-1，`loudness.rs` | MIT |
| 音频文件读写（已有） | `hound` / `symphonia` | 已在用 | — |
| MIDI 读写（已有） | `midly = "0.5"`（`Cargo.toml:68`） | 已在用 | — |
| PNG 输出（频谱图） | `image` | Phase 5-1 | MIT/Apache-2.0 |
| FFT（已有） | 工程内 `utai-dsp\src\stft.rs` | 已在用 | — |

**只有 `ebur128` 是真正的新增依赖。** 其余能力工程里都已经有基础设施。这是个好消息——Phase 1 的核心工作量不在找库，在接线和验证。

**为什么不自己写 LUFS**：EBU R128 的 K 加权滤波器系数、门限（-70 LUFS 绝对门 + -10 LU 相对门）、3 秒短时窗、块重叠 75% 这些细节实现错一个数值就整体偏，而参考实现已经被广泛校验过。这是典型的"该用库的地方"。

**为什么 dither 要自己写**：反过来，dither 只有几十行，而且需要和工程自己的位深转换路径（`export_audio.rs:178` 的 `*32767.0 as i16`、`:182` 的 `*8_388_607.0 as i32`）严格配套。引第三方库反而要适配，不如自己写。

### 10.2 音源库（用户"多挂音源"需求的弹药）

这些不是代码，是采样数据，走 `assets.rs:149-161` 现成的资产包下载机制加条目即可（Phase 3-8）。

| 音源库 | 类型 | 体积量级 | 强项 | 许可（需核验） |
|---|---|---|---|---|
| FluidR3 GM | SF2 通用 MIDI | ~148MB | 全音色覆盖，已在工程条目里 | MIT 系 |
| GeneralUser GS | SF2 通用 MIDI | ~32MB | 体积小质量高，已在条目里 | 自定义（允许分发） |
| Arachno SoundFont | SF2 通用 MIDI | ~155MB | 音色更现代，已在条目里 | 自定义 |
| **Salamander Grand Piano** | SFZ 钢琴 | ~1GB | **16 层力度**，钢琴质量天花板级 | CC-BY |
| **VSCO 2 Community Edition** | SFZ 管弦 | ~3GB | **CC0**，管弦最省心的选择 | CC0 |
| Sonatina Symphonic Orchestra | SFZ 管弦 | ~500MB | 体积友好的管弦 | CC-BY |
| Virtual Playing Orchestra | SFZ 管弦 | ~400MB | 基于多个免费库重制，可用性好 | 自定义（免费） |
| Black and Green Pianos | SFZ 钢琴 | ~200MB | 与 Salamander 分层叠加用 | 免费 |
| Karoryfer 系列（贝斯/人声/打击） | SFZ | 各 100–500MB | 贝斯质量突出 | CC-BY / 自定义 |
| SM Drums / 免费鼓库 | SFZ 鼓 | ~200MB | 配合 `drumDetection.ts` 重渲鼓组 | 各异 |

**下载策略建议**：这些库加起来几个 GB，**绝对不能打进安装包**。参照 `全功能融合规划.md:108` 的既定策略（模型 19GB+ 与 venv 7.5GB 不打包）和 `:192-200` FFmpeg 按需下载的做法，全部走按需下载。UI 上按"钢琴/管弦/鼓/贝斯/通用"分组，标体积和许可。

**分层推荐配方**（Phase 3-1 落地后可直接用）：

| 目标音色 | 层 1（主） | 层 2（-9dB） | 层 3（-15dB） |
|---|---|---|---|
| 厚实钢琴 | Salamander | Black and Green | FluidR3 GM piano |
| 温暖弦乐 | VSCO 2 CE strings | Sonatina strings | — |
| 有力鼓组 | SM Drums | VSCO 2 CE percussion | — |
| 饱满贝斯 | Karoryfer bass | GeneralUser GS bass | — |

分层的关键在**第 2、3 层要压得足够低**（-9dB 以下）。它们的作用是补充第 1 层缺失的谐波和瞬态密度，不是"再加一个音色"。压得不够就变成两个乐器在打架，反而更假。

### 10.3 必须自己做的（市面无现成开源）

| 功能 | 为什么没有现成的 | 实现方法 |
|---|---|---|
| `melodyRestructure` 五法 | 这是本工程独有的原创化需求，学界有旋律变奏研究但无可直接用的库 | 第 4.2 节已给五法算法描述；核心是 `Note[]` 数组的纯函数变换，TypeScript 实现即可 |
| 旋律相似度度量 | 商业相似度检测系统都是闭源的 | n-gram（音程序列，n=3~5）Jaccard 相似度 + 旋律轮廓（升/降/平的符号序列）相关系数，两者加权 |
| `humanize` 可复现抖动 | 各家 DAW 都有但都是闭源 | xorshift32 种子化 PRNG（参照 `effectsBus.ts:61-81`），按轨预设查表（8.3 节那张表） |
| `formantJitter` 全通实现 | 现成的共振峰处理都走 WORLD/相位声码器（L4），本规划要 L3 | 8.7 节已给公式：级联 4–6 级一阶全通，系数被 0.3–1.2Hz LFO 调制 |
| `breathLayer` | 气息采样需要自己录，插入逻辑需要配合 `notes` | 符号域检测 gap（>400ms 的音符间隙）→ 在 gap 起点前 120ms 叠入吸气采样，音量按前一音符 velocity 缩放 |
| 累积损伤 SNR 估算 | 本规划独有的概念 | 每个节点类型声明典型 SNR，引擎沿 DAG 拓扑序累加，公式见 3.3 节 |
| `complianceCheck` 聚合报告 | 各家母带工具都有但都闭源 | 6.8 节已给 JSON 结构；逐项调 Phase 1 的测量命令再汇总 |
| SF2 低通滤波器 | FluidSynth 有实现但是 LGPL 且是完整合成器 | SF2 规范公开，双二次滤波器 + `8.176*2^(cents/1200)` 换算，几十行 |
| 多音源分层调度 | 属于宿主层设计，无库可抄 | 同一份 `RenderNote[]` 喂 N 次 `render_soundfont_notes`（`soundfont.rs:67-76`），各带 gain/pan，再 merge |

**关于"可以抄 FluidSynth"这件事**：FluidSynth 是 LGPL-2.1。工程里已经以**外部可执行文件**方式用它（`amt_export.rs:34` 指向 `fluidsynth/2.5.6/bin/fluidsynth.exe`），这种进程调用方式是 LGPL 友好的。但**把它的源码抄进 `synth.rs`** 是另一回事，会给整个工程带上 LGPL 义务。所以 Phase 3-5 的滤波器要照**规范**自己写，不要照**它的源码**写。

### 10.4 明确不引入的

| 那两份文档提到的 | 为什么不引入 |
|---|---|
| `fakeprint` 包 | 1.2 节已核实：这个包在工程里零命中，且我在公开生态里找不到可信来源。**来源不明的包不能进工程。** |
| DeepFilterNet | 工程 zero-hit，且降噪不是本规划的需求路径（本规划的人声是自己生成的，没有噪声要降） |
| `lofty`（元数据读写库） | 它本身是正当的库，但那两份文档引入它的目的是"清除 AI 元标签"，这是第 0 章明确不做的。**如果将来有正当的元数据需求（写标题/艺术家/ISRC），再单独评估引入。** |
| `pyloudnorm` | Python 依赖，本工程 LUFS 走 Rust 的 `ebur128` 更合适，不必再拉一个 Python 侧实现 |
| 任何"AI 检测规避"工具链 | 第 0 章 + 第 11 章 |

---

## 第 11 章 · 明确不做项（以及为什么）

这一章存在的意义是：**把"不做"也当成规划的一部分写清楚**，免得将来有人（包括未来的我）以为是遗漏而补上去。

### 11.1 AI 元标签清除工具

**不做。** 这是用户明确点名的需求，我明确拒绝，理由：

- **国内**：《人工智能生成合成内容标识办法》自 2025-09-01 施行，要求 AI 生成合成内容添加显式与隐式标识，**任何人不得恶意删除、篡改、伪造、隐匿标识**，也不得提供此类工具。抖音等平台对恶意规避标识的处置包括永久封号。
- **欧盟**：AI 法案的透明度义务自 2026-08-02 起适用，要求 AI 生成内容以机器可读方式标记。
- **工具提供方责任**：注意法规不只约束使用者，也约束**提供工具的人**。把这个功能做进工程，工程本身就成了违规工具。

**替代方案（本规划的做法）**：配方 B/C/E 的核心手法是**只取符号，丢掉 AI 音频**。最终输出文件是本地渲染器新生成的，**它从来没有被 AI 生成过，所以没有 AI 标识要清除**。这不是绕过法规，是根本不进入法规适用的情形——因为最终作品确实是你自己做的。

**这一点值得多说一句**：想清除 AI 标签，本质上是想"声称一个 AI 作品是我的"。而本规划让你**真的把它变成你的**。前者是伪装，后者是事实。工作量后者大一些，但它换来的是没有任何风险敞口的作品，还顺带把音质提升了一个数量级。

### 11.2 以规避检测为目的的音频处理

**不做为"规避手段"，但相关 DSP 工具会作为正当工具提供。**

| 用户/文档点名的 | 本规划的处理 |
|---|---|
| 调速（speedShift） | ✅ 保留为音乐工具（改速度就是改速度），❌ 不宣传为防检测 |
| 音频变调（transposeAudio） | ✅ 保留为音乐工具，❌ 不宣传为防检测；且 UI 提示"优先在 MIDI 域移调" |
| QPA 量子相位对齐 | ✅ 做成 Phase 4-5 的"峰值余量优化"工具（这是它的真实用途），❌ 不宣传为防检测 |
| SDF 频谱动态折叠 | ✅ 折进 `busEq` / 多段动态（正当母带工具），❌ 不单独做成"折叠"节点 |
| SFR 空间特征重构 | ✅ 做成 Phase 4-2 `stereoWidth`（正当母带工具） |
| PTR 感知时重采样 | ❌ 不做。它就是变速换个名字，而变速已有 `speedShift` |
| HRR 谐波随机再生 | ✅ 折进 Phase 4-3 `saturate` + Phase 7-2 `voiceRealism`（正当工具） |
| 破坏指纹 | ❌ 不做。铁律二：对版权指纹无效，且这是明确的规避意图 |
| 添加干扰音频（超声/白噪/次声） | ❌ 不做。3.6 节论证过零收益纯损伤 |
| 硬削波（用户说的"肖波"） | ❌ 不做为"处理手段"。✅ `peak_guard`（`audio\mod.rs:510-547`，已有）是真正无损的峰值管理；✅ `saturate` 软饱和作为艺术选择 |
| "一键转原创"总开关 | ❌ 不做。任何把 L3/L4 处理打包成一键的 UI 都在鼓励用户白掉音质 |

**关于"工具中立"的界线**：我保留这些 DSP 是因为它们在音乐制作里有真实用途——变速是为了改编排，变调是为了适配音域，饱和是为了染色。我拒绝的是**把它们包装成防检测功能**。同一段代码，写"提升音质余量"和写"破坏 AI 指纹"，法律和道德性质完全不同。**UI 文案在这里不是修辞问题，是定性问题。**

### 11.3 `deepOriginal` 节点的处置建议

这个节点现在是"调速 + 变调"的组合（`engine.ts:1106-1124`），名字直接叫"深度原创"，这是最需要处理的一处。

**建议**：**改名 + 拆解**，不要保留"deepOriginal"这个名字。

| 动作 | 理由 |
|---|---|
| 名字改成中性描述（如"时间/音高变换"） | 现在的名字在暗示"用这个就原创了"，这是误导，而且它做的事对原创度贡献接近零 |
| 功能拆成 `speedShift` + `transposeAudio` 两个已有节点 | 组合节点没有额外价值，反而隐藏了"这里发生了两次 L4" |
| 保留旧类型做预设兼容（内部映射到两个新节点） | 用户已存的工作流预设不能坏 |
| UI 明确标 🔴L4 + 累积 SNR 提示 | 让用户看到代价 |

**顺带修掉 Phase 0-4 那两个空 catch**（`:1114,1121`）——现在它失败了完全静默，用户以为"深度原创已生效"，实际什么都没发生。这个组合（误导性名称 + 静默失败）是整个工程里最需要处理的一处。

### 11.4 不做的其他项

| 项 | 理由 |
|---|---|
| 引入大模型做音频修复/增强 | `全功能融合规划.md:354-356` 原作者已有明确理由不做，尊重原设计 |
| `presetFallbacks = true` | `msst-catalog.ts:761-767` 的 UN-HASHED 注释明确要求 false，别动 |
| `drag_drop_enabled(false)` | 会废掉现有 OS 文件拖入导入能力（前期已确认的坑） |
| 推翻 AMT 的 Python sidecar 方案 | `AMT-MIDI融合规划.md:67-68,78` 已论证 Transformer 无法转 ONNX，方案 A 全量 Rust 被否决 |
| RVC 与 SoVITS 的 rmvpe 权重互换 | `assets.rs:82,86` 两条血统架构不同，不可互换 |

---

## 第 12 章 · 编码纪律与验收标准

这一章是给 TRAE（以及任何接手这份规划的人）看的执行约束。**违反这里任何一条，PR 都不该合。**

### 12.1 硬性纪律（不可协商）

| # | 纪律 | 为什么 | 怎么验 |
|---|---|---|---|
| 1 | **所有新文案走 i18n，禁止硬编码中文** | `NodePalette.tsx:44-58` 已经踩过这个坑（AI/效果类硬编码中文，song 族才走 i18n），不能再加新的 | i18n parity 脚本；grep 新增 `.tsx` 里的中文字面量 |
| 2 | **i18n 三语同步（zh / en / ja）** | 缺一种语言就会显示 key 名 | parity 脚本比对三份 key 集合 |
| 3 | **新增节点必须改齐 6 处**（7.7 节 checklist） | 漏任何一处都是"静默失败"或"用户不可达" | 逐项人工过 checklist |
| 4 | **每个音频处理节点必须声明损伤等级** | 6-5 的累积 SNR 估算依赖它；也是本规划的核心主张 | 类型上做成必填字段，漏了编译不过 |
| 5 | **禁止空 catch** | `engine.ts:1114,1121` 的教训 | lint 规则 + review |
| 6 | **switch 必须有 default** | `engine.ts:1393` 的教训 | TS `noImplicitReturns` / lint |
| 7 | **随机数必须种子化** | 试听与导出必须一致（2-1 要点） | 单测：同种子两次调用结果逐字节相同 |
| 8 | **不得修改原作者已注释说明的约定** | `demucs.rs:1-17` 的 `_spec` 约定、`msst-catalog.ts:761-767` 的 UN-HASHED、`assets.rs:143-147` 的非商用许可、`formant.rs:1-12` 的 WORLD-free、`NodeShell.css:195` 的配色规约 | review 时对照 1.5 节那 6 条 |
| 9 | **美观度：简洁排第一** | 用户明确要求 | 不加装饰性元素，不加渐变堆叠，信息密度优先 |
| 10 | **代码检测优先于界面测试** | 用户明确要求 | 先跑 tsc/cargo/vitest 全绿，界面留给用户手动 |

### 12.2 每个 Phase 的通用验收命令

```powershell
# 1. Rust 侧
cargo check --manifest-path src-tauri\Cargo.toml

# 2. 前端类型 + 构建（package.json:10 已配 "tsc -b && vite build"）
npm run build

# 3. 单元测试（package.json:12 已配 "vitest run"）
npm test
```

**三条全绿才算完成**。当前基线是 **617 passed / 4 skipped**（跳过的是 4 个手动 dump 用例），新 Phase 只能增加通过数，不能减少。

**注意**：`npx vitest run --reporter=basic` 会失败（这个 vitest 版本没有 `basic` reporter），直接 `npx vitest run` 或 `npm test`。

### 12.3 DSP 类新功能的额外验收要求

音频 DSP 最容易出的问题是"看起来跑通了，实际数值是错的"。所以本规划要求每个 DSP 模块都有**数值级单测**，不只是"不崩":

| 模块类型 | 必须的数值验收 |
|---|---|
| 测量类（LUFS / true peak / 相位相关性） | 用已知答案的合成信号验证（如 1kHz 正弦 -20dBFS 满幅，LUFS 应为已知值），误差 < 0.1 |
| 纯增益类（`lufsNormalize` / `peak_guard`） | 输出/输入逐样本比值必须是**单一常数**（这是"无损"的定义） |
| 线性滤波类（`busEq` / `dcOffsetRemove`） | 扫频信号测频响曲线，与理论传递函数比对 |
| M/S 类（`stereoWidth`） | width=1.0 时输出必须与输入**逐样本完全相同**（恒等性测试） |
| 非线性类（`saturate`） | 单频输入测 THD，确认与 drive 参数单调对应 |
| dither | 残差分布检验（TPDF）+ 能量检验（≈ -110dBFS） |
| 符号域变换（`humanize` / `melodyRestructure`） | 音符数量守恒（除法五）、时间无负值、key 在 0–127、种子可复现 |

**`stereoWidth` 的恒等性测试特别重要**。M/S 矩阵来回变换如果有系数错误，width=1.0 时会有微小差异，这种 bug 在听感上几乎察觉不到，但会污染所有走这条链的作品。**"参数中性位置必须是逐样本恒等"是本规划对所有可调 DSP 的强制要求。**

### 12.4 文档与提交纪律

| 项 | 要求 |
|---|---|
| 不主动创建 `.md` 文档 | 除用户明确要求外（本文档是用户明确要求的） |
| 提交信息说清"为什么"而非"改了什么" | diff 已经说明改了什么 |
| 不 `git add .` | 逐个文件加，避免带进 `.env` / 大二进制 |
| 不改 git config，不 force push | — |
| 新依赖用精确版本 | 参照 `Cargo.toml:68` 的 `midly = "0.5"` 风格 |

---

## 附录 A · 全文核心结论速查

如果只看一页，看这页。

**1. 三个"检测"完全不是一回事，别混为一谈**

| | 检出率 | 后果 | 本规划的解法 |
|---|---|---|---|
| 版权指纹 | ★★★★★ | 下架/分成划走/封号 | **不让第三方波形进输出** |
| AI 生成检测 | ★★☆☆☆ | 打标签 | **输出确实不是 AI 生成的** |
| 相似度抄袭 | ★★★☆☆ | 侵权索赔 | **旋律重构 ≥55%** |

**2. 损伤等级差异是数量级的，不是百分比的**

| 操作 | 符号域 | 音频域 |
|---|---|---|
| 移调 | 🟢L0 无损 | 🔴L4 ~35dB |
| 变速 | 🟢L0 无损 | 🔴L4 ~35dB |
| 换音色 | 🟢L0（换音源重渲） | 🔴L4（RVC） |

**能在符号域做的事，绝不在音频域做。** 这一条能解决 90% 的音质问题。

**3. 累积损伤由最差那一环支配**

`总SNR ≈ -10·log10( Σ 10^(-SNR_i/10) )`

1 个 L4（35dB）+ 5 个精细 L2（各 115dB）= **34.99dB**。所以精细母带在一条有 L4 的链上毫无意义。**先去掉 L4，再谈母带。**

**4. 截图那 6 个参数的真实身份**

QPA = 全通相位旋转（有正当用途：峰值余量）｜SDF = 频谱包络扰动｜SFR = 立体声矩阵+去相关｜PTR = 变速换名字｜HRR = 谐波激励。**全部改波形，全部有损伤，对版权指纹全部无效。** 花掉确定的音质，买不确定的效果。

**5. 用户点名的"肖波"（削波）判断是反的**

硬削波 THD 1–10%，是本文档列举的所有处理里损伤最大的之一。真正无损的峰值管理是 `peak_guard`（工程已有，`audio\mod.rs:510-547`，全曲单标量只压不抬）。

**6. 唯一同时解决版权 + AI 检测 + 音质的路线**

**别改波形，重新演奏。** 音符自己改，伴奏本地音源重渲，人声自己的模型唱。输出里每个采样点都是本地新算的——不需要"破坏指纹"，因为**压根没有那个指纹**。

**7. 优先级**

Phase 0 修 bug → Phase 1 LUFS/dither/merge/体检 → Phase 2 humanize/velocityCurve/melodyRestructure。这三样做完，配方 A/B/C/E 全部可跑，发行级质量。

---

## 附录 B · 与那两份文档的关键分歧

| 议题 | 那两份文档 | 本规划 | 依据 |
|---|---|---|---|
| AI 元标签清除 | 列为核心功能 | **拒绝实现** | 第 0.2 节法规 |
| 防检测处理 | 大量 L3/L4 处理 | **拒绝作为防检测手段**，保留为音乐工具 | 第 3 章 + 11.2 |
| `run_ai_check` / `runtime/ai_check/*.py` | 当作已有基础 | **工程里零命中**，根本不存在 | 1.2 节核实 |
| `fakeprint` 包 | 列为依赖 | **来源不明，拒绝引入** | 10.4 节 |
| DeepFilterNet / `lofty` / `pyloudnorm` | 列为依赖 | 不引入（各有理由） | 10.4 节 |
| 无损导出 | 列为"待新增" | **已存在**（6 格式，`export_audio.rs`） | 1.1 节核实 |
| Whisper 歌词提取 | 列为"待新增" | **已存在**（`amt_lyrics.rs`） | 1.1 节核实 |
| FluidSynth | 列为"待新增" | **已存在**（`amt_export.rs:34`），只是只服务 AMT | 1.1 节核实 |
| 多语言 G2P | 列为"待新增" | **已存在**（7 语言，`g2p.rs:42-88`） | 1.1 节核实 |
| 本地训练链 | 列为"待新增" | **已存在**（13 命令 + 4 条 pipeline） | 1.1 节核实 |
| 工作流预设导入导出 | 列为"待新增" | **已存在**（`storage.rs:862-893`） | 1.1 节核实 |
| 原创化的主战场 | 音频域处理 | **符号域** | 第 4 章 |
| 音质保障手段 | "选择损伤小的处理" | **减少音频域处理次数** | 3.3 节累积公式 |
| LUFS / EBU R128 | 未提及或轻描淡写 | **列为最高优先级新功能** | Phase 1 |
| 分析节点权重 | 低 | **高（零损伤纯收益，支撑"先测后处理"）** | 8.7 节 + Phase 5 |

**总的分歧点只有一句话**：那两份文档在回答"怎么让检测测不出来"，本规划在回答"**怎么做出真正是自己的、能发行的、音质好的作品**"。后者顺带解决了前者，且没有法律风险。

---

## 附录 C · 编码纪律速查（贴在显示器上那种）

```
① 新文案 → i18n，三语同步，禁硬编码中文
② 新节点 → 6 处注册，一处不漏
③ 音频节点 → 必须声明损伤等级
④ 能在符号域做 → 绝不在音频域做
⑤ 随机数 → 必须种子化
⑥ 空 catch → 禁止
⑦ switch → 必须有 default
⑧ 参数中性位置 → 必须逐样本恒等
⑨ 原作者注释的约定 → 不动
⑩ 交付前 → cargo check + npm run build + npm test 三绿
```

---

*本规划基于对 `e:\软件开发\UtaiSynthesizer-main\UtaiSynthesizer-main` 的逐文件代码核实撰写。文中所有带行号的引用均为实际读取所得；所有"零命中"结论均为实际全量搜索所得。许可证信息与法规条款请在实施前自行复核。*



